---
phase: quick-260606-c24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/forge/src/index.ts
  - apps/forge/test/create-task.test.ts
  - apps/filer/wrangler.jsonc
  - apps/herald/wrangler.jsonc
  - apps/forge/wrangler.jsonc
  - apps/sundial/wrangler.jsonc
  - apps/compass/wrangler.jsonc
autonomous: true
requirements: [WEEKLY-01, CORE-01]
must_haves:
  truths:
    - "env.FORGE.createTask exists on the Forge WorkerEntrypoint and is callable (no TypeError)"
    - "A Headhunter-shaped createTask call writes a real D1 task row and emits the per-task Wire event"
    - "A replay of the same createTask (same idempotencyKey) is a no-op — no duplicate row, counter unchanged"
    - "createTask reuses Forge's existing upsertTask/taskIdFor/buildTaskEvent/DO-lock path — no duplicated dedupe logic"
    - "All 5 Phase-1 wranglers carry database_id e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3 on their atlas-db d1_databases entry"
  artifacts:
    - path: "apps/forge/src/index.ts"
      provides: "Forge.createTask WorkerEntrypoint method delegating to the task pipeline"
      contains: "async createTask"
    - path: "apps/forge/test/create-task.test.ts"
      provides: "binding-symmetry + replay regression test for createTask"
      contains: "createTask"
    - path: "apps/filer/wrangler.jsonc"
      provides: "shared atlas-db database_id"
      contains: "e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3"
    - path: "apps/herald/wrangler.jsonc"
      provides: "shared atlas-db database_id"
      contains: "e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3"
    - path: "apps/forge/wrangler.jsonc"
      provides: "shared atlas-db database_id"
      contains: "e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3"
    - path: "apps/sundial/wrangler.jsonc"
      provides: "shared atlas-db database_id"
      contains: "e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3"
    - path: "apps/compass/wrangler.jsonc"
      provides: "shared atlas-db database_id"
      contains: "e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3"
  key_links:
    - from: "apps/headhunter/src/index.ts"
      to: "apps/forge/src/index.ts Forge.createTask"
      via: "env.FORGE.createTask service-binding RPC"
      pattern: "async createTask"
    - from: "apps/forge/src/index.ts Forge.createTask"
      to: "@atlas/tasks upsertTask"
      via: "reused dedupe/merge pipeline"
      pattern: "upsertTask"
---

<objective>
Close the two confirmed v1.0 milestone integration gaps found by `.planning/v1.0-MILESTONE-AUDIT.md`:

1. **INT-BLOCKER-1 (WEEKLY-01):** Implement `Forge.createTask(task, opts)` on the Forge `WorkerEntrypoint` so Headhunter's `env.FORGE.createTask(...)` calls (apps/headhunter/src/index.ts:204 and :306) stop throwing `TypeError: env.FORGE.createTask is not a function`. The method MUST delegate to Forge's EXISTING task pipeline (`taskIdFor` / `upsertTask` / `buildTaskEvent` / `contentHashOf` / the `FORGE_LOCK` DO + `send`), never duplicate dedupe logic.
2. **INT-WARN-1 (CORE-01):** Add the shared `database_id` `"e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3"` to the 5 Phase-1 production wranglers (filer/herald/forge/sundial/compass) that declare `database_name: "atlas-db"` but omit the id, so `wrangler deploy` binds the shared atlas-db instead of an isolated D1.

Purpose: WEEKLY-01 (Headhunter→Forge apply-by tasks) is marked "satisfied" but is broken at runtime; the deploy gap would split production task storage so Sundial/Compass can't read Forge's tasks. Both must close regardless of remaining phases.

Output: A working `Forge.createTask` with a real binding-symmetry + replay regression test, and 5 corrected wranglers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@CLAUDE.md

@apps/forge/src/index.ts
@apps/headhunter/src/index.ts
@packages/tasks/src/store.ts
@packages/tasks/src/dedupe.ts
@apps/forge/test/wire.test.ts
@apps/forge/test/lock.test.ts
@apps/forge/test/apply-migrations.ts
@apps/steward/wrangler.jsonc

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

CALLER shape Headhunter passes (apps/headhunter/src/index.ts:55-67) — createTask MUST accept EXACTLY this:
```typescript
createTask(
  task: {
    title: string;
    due: string | null;
    due_kind: "explicit" | "inferred";
    source_agent: string;   // Headhunter passes "Headhunter"
    thread: null;           // job tasks have NO Gmail thread
    priority: string;       // Headhunter passes "P1" | "P2"
  },
  opts: { idempotencyKey: string; runId: string },
): Promise<{ id: string } | null>;
```
The structured idempotencyKey Headhunter supplies is `headhunter:window:<co>:<cycle>:<role>` (apps/headhunter/src/windows.ts:249). runId is the owner-local date string (YYYY-MM-DD).

EXISTING Forge pipeline pieces to REUSE (apps/forge/src/index.ts) — do NOT reimplement:
```typescript
export function taskIdFor(key: string): string;                 // `task-${key}`
export function buildTaskEvent(taskId, action: UpsertAction, mergeKey?: string): WireEvent;
export async function contentHashOf(priority: string|null, due: string|null): Promise<string>;
// imported into index.ts:
import { upsertTask, type TaskInput, type UpsertAction } from "@atlas/tasks";
import { send } from "@atlas/wire";
import { localDate } from "@atlas/shared";
// the optional DO lock, addressed by run date:
this.env.FORGE_LOCK?.getByName(date).withLock(fn)
```

EXISTING @atlas/tasks contract (packages/tasks/src/store.ts):
```typescript
interface TaskInput { id; dedupe_key; thread: string|null; title; priority: string|null;
  due: string|null; due_kind: string|null; status?; source_agent?: string|null;
  locked_by_owner?; subtasks?; }
upsertTask(db, task): Promise<{ action: "inserted"|"merged"|"noop"; id: string }>;
// dedupeKey(thread, normalizedTitle, dueDate) requires a STRING thread — but Headhunter
// tasks carry thread: null. Use the supplied STRUCTURED idempotencyKey as the dedupe basis
// (it is already the stable per-window identity), NOT dedupeKey().
```

How runMorning emits per-task events (apps/forge/src/index.ts:219-236) — mirror this exactly:
- inserted → `send(env, buildTaskEvent(id, "inserted"))`           (op:increment, key=taskId)
- merged   → `send(env, buildTaskEvent(id, "merged", mergeKey))`   (op:upsert,    key=task:<id>:<mergeKey>)
- noop     → emit NOTHING (owner-locked or same-state replay)
mergeKey on a merge = `await contentHashOf(priority, due)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Forge.createTask + binding-symmetry/replay regression test</name>
  <files>apps/forge/src/index.ts, apps/forge/test/create-task.test.ts</files>
  <behavior>
    - Test 1 (binding symmetry — the method EXISTS): `typeof new Forge(ctx, env).createTask === "function"`. This is the regression guard for INT-BLOCKER-1 — Headhunter's tests stubbed FORGE so the method's absence went unnoticed by the whole Phase-2 suite.
    - Test 2 (real D1 task + Wire event): calling createTask with the exact Headhunter shape (`{title, due, due_kind, source_agent:"Headhunter", thread:null, priority}` + `{idempotencyKey:"headhunter:window:acme:fall2026:swe", runId:"2026-06-10"}`) returns `{id: <taskId>}`, writes a real row into the `tasks` table readable by `SELECT ... WHERE id=?`, and the canonical insert event (`buildTaskEvent(id,"inserted")`, op:increment, key=taskId) is the one emitted. Assert the Wire-contract via buildTaskEvent + `WireEvent.parse` and/or applyEvent (DoD test 1 + 2).
    - Test 3 (replay = no-op): a second createTask with the SAME idempotencyKey + same task fields produces NO duplicate task row (a `SELECT COUNT(*)` on the dedupe_key stays 1) and the action is not a fresh insert. Confirm replaying the emitted insert event through `applyEvent` from `@atlas/steward-core` returns `{applied:false}` and the `tasks_open` counter is unchanged (mirror wire.test.ts:32-43).
    - Test 4 (null return contract): exercise the `{id}|null` return — createTask returns `{id}` for inserted/merged and `null` only for noop (owner-locked). Keep it simple: assert the `{id}` happy path returns a non-null id; a noop-producing call (e.g. against an owner-locked dedupe_key) returns null.
  </behavior>
  <action>
Add `async createTask(task, opts)` to `class Forge extends WorkerEntrypoint<Env>` in apps/forge/src/index.ts (closes INT-BLOCKER-1, requirement WEEKLY-01). Signature MUST match the caller exactly (see the interfaces block): `createTask(task: {title:string; due:string|null; due_kind:"explicit"|"inferred"; source_agent:string; thread:null; priority:string}, opts: {idempotencyKey:string; runId:string}): Promise<{id:string}|null>`. Do NOT invent a new shape; you may extract a named exported param type if it improves clarity, but the structural contract MUST stay identical so Headhunter's typed `FORGE` binding still satisfies it.

Implementation MUST delegate to Forge's existing pipeline — no duplicated/divergent dedupe logic (Pillar 1, Pillar 5):
- Derive the id from the SUPPLIED structured `opts.idempotencyKey` (Headhunter tasks have `thread:null`, so `dedupeKey(thread,…)` does not apply — the window idempotencyKey IS the stable per-window identity). Compute `const id = taskIdFor(opts.idempotencyKey)` so a re-run re-derives the SAME id.
- Build a `TaskInput`: `{ id, dedupe_key: opts.idempotencyKey, thread: null, title: task.title, priority: task.priority, due: task.due, due_kind: task.due_kind, status: "open", source_agent: task.source_agent, locked_by_owner: 0, subtasks: [] }`.
- Run the dedupe+write inside the FORGE_LOCK DO when present, addressed by `opts.runId` (the run date) — exactly like morning() does (apps/forge/src/index.ts:270-281): `const lock = this.env.FORGE_LOCK; const runUnderLock = lock ? <T>(fn:()=>Promise<T>)=>lock.getByName(opts.runId).withLock(fn) : <T>(fn:()=>Promise<T>)=>fn();`. Call `upsertTask(db, input)` INSIDE that closure and return its `{action,id}`. Keep `send()` OUTSIDE the lock (mirror runMorning: the lock guards only dedupe+write; the for-loop that sends runs after runUnderLock returns).
- Access DB the same way morning() does: `const db = (this.env as unknown as { DB: D1Database }).DB;`.
- Emit per the upsert action, mirroring runMorning (apps/forge/src/index.ts:219-236): `inserted` → `await send(this.env, buildTaskEvent(id, "inserted"))`; `merged` → `await send(this.env, buildTaskEvent(id, "merged", await contentHashOf(task.priority, task.due)))`; `noop` → emit nothing.
- Return `{ id }` for `inserted` | `merged`; return `null` for `noop` (the `{id}|null` contract).
- Do NOT add a queues.consumers block, do NOT write the tasks table by hand, do NOT place a secret in any field, do NOT re-fetch anything. This is a Wire PRODUCER path only — Steward stays the sole atlas-wire consumer (Pillar 1).

Create apps/forge/test/create-task.test.ts following house style (vitest + `cloudflare:test`, `createExecutionContext()` + `new Forge(ctx, env)` as in lock.test.ts, real D1 via the existing test/apply-migrations.ts setup — auto-applied by vitest.config.ts). Implement the four behaviors above. For Wire-event/replay assertions follow wire.test.ts (use `buildTaskEvent`/`taskIdFor` + `applyEvent` from `@atlas/steward-core`) so the DoD Wire-contract + replay-through-Steward assertions are both present. Do NOT add a Flagger failure-path test (out of scope for this fix).
  </action>
  <verify>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && pnpm --filter @atlas/forge test 2>&1 | tail -30</automated>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && pnpm --filter @atlas/forge typecheck 2>&1 | tail -15</automated>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && grep -c "async createTask" apps/forge/src/index.ts</automated>
  </verify>
  <done>Forge.createTask exists on the WorkerEntrypoint with the exact caller signature, delegates to taskIdFor/upsertTask/buildTaskEvent/FORGE_LOCK/send (no duplicated dedupe), returns {id}|null; create-task.test.ts passes within a green @atlas/forge suite (binding-symmetry + real-D1-task + replay-no-op + null-return), and @atlas/forge typecheck is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared database_id to the 5 Phase-1 wranglers</name>
  <files>apps/filer/wrangler.jsonc, apps/herald/wrangler.jsonc, apps/forge/wrangler.jsonc, apps/sundial/wrangler.jsonc, apps/compass/wrangler.jsonc</files>
  <action>
Close INT-WARN-1 (requirement CORE-01). In each of the 5 production wranglers, add the line `"database_id": "e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3",` to the `d1_databases` entry, placed immediately AFTER the `"database_name": "atlas-db",` line and BEFORE `"migrations_dir"` — matching the exact ordering and formatting Steward uses (apps/steward/wrangler.jsonc:47-54). Use the Edit tool per file, not a script. Touch ONLY the d1_databases block; change nothing else. The id `e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3` is the shared atlas-db every Phase-0/2 app already carries. Note: the per-app `wrangler.test.jsonc` files intentionally OMIT the id (miniflare uses an in-memory D1) — do NOT edit those.
  </action>
  <verify>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && grep -l "e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3" apps/filer/wrangler.jsonc apps/herald/wrangler.jsonc apps/forge/wrangler.jsonc apps/sundial/wrangler.jsonc apps/compass/wrangler.jsonc | wc -l | tr -d ' '</automated>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && for f in filer herald forge sundial compass; do node -e "const s=require('fs').readFileSync('apps/$f/wrangler.jsonc','utf8'); JSON.parse(s.replace(/^\s*\/\/.*$/gm,'').replace(/\/\*[\s\S]*?\*\//g,'')); console.log('$f OK')"; done</automated>
  </verify>
  <done>All 5 wranglers (filer/herald/forge/sundial/compass) carry "database_id": "e7fee76c-2e3d-486e-8a8a-fd1aec6a5af3" in their atlas-db d1_databases entry (grep count = 5), each file still parses as valid JSONC, and the per-app wrangler.test.jsonc files are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Headhunter → Forge (service binding RPC) | Headhunter-supplied task fields (title/due/priority) + idempotencyKey cross into Forge's D1 write path |
| Forge → atlas-wire (Queue producer) | Per-task event emitted onto the bus for Steward to consume |
| wrangler.jsonc → production D1 binding | The database_id determines which D1 instance the deployed Worker binds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-c24-01 | Tampering | Forge.createTask D1 write | mitigate | Reuse upsertTask (positional `?` binds only — no SQL interpolation; T-1-D1-01 surface preserved); never hand-write the tasks table |
| T-c24-02 | Elevation of Privilege | Forge as a Wire writer | mitigate | createTask is a PRODUCER-only path; add NO queues.consumers block — Steward stays the sole atlas-wire consumer (Pillar 1, hard CI failure if violated) |
| T-c24-03 | Information Disclosure | Task fields carrying a secret | accept | Headhunter sources titles from public board/window data with no email body; no 2FA/reset content reaches createTask. No new redaction surface introduced by this fix. |
| T-c24-04 | Denial of Service / data divergence | Replay creating duplicate tasks | mitigate | Dedupe on the structured opts.idempotencyKey via upsertTask's unique index → replay is a no-op (Pillar 5); replay test asserts meta.changes===0 / count stays 1 |
| T-c24-05 | Tampering | Wrong/isolated production D1 binding | mitigate | Add the canonical shared database_id to all 5 wranglers so deploy binds the single atlas-db (not a split store); JSONC-parse verify guards malformed edits |
| T-c24-SC | Tampering | npm/pnpm installs | accept | No new packages added (constraint); no install step in this plan — supply-chain surface unchanged |
</threat_model>

<verification>
- `pnpm --filter @atlas/forge test` is green (existing suite + the new create-task.test.ts).
- `pnpm --filter @atlas/forge typecheck` is clean (the createTask signature satisfies Headhunter's typed FORGE binding).
- `grep -c "async createTask" apps/forge/src/index.ts` == 1 (the method exists — INT-BLOCKER-1 closed).
- All 5 wranglers contain the shared database_id and parse as valid JSONC (INT-WARN-1 closed).
- Sanity: `pnpm -r typecheck` repo-wide stays clean (no regression introduced).
</verification>

<success_criteria>
- INT-BLOCKER-1 closed: `Forge.createTask(task, opts)` exists on the WorkerEntrypoint with the exact shape Headhunter calls; at runtime `env.FORGE.createTask(...)` writes a real D1 task + emits the per-task Wire event, no TypeError. WEEKLY-01 Headhunter→Forge is functional.
- createTask reuses the existing pipeline (taskIdFor/upsertTask/buildTaskEvent/contentHashOf/FORGE_LOCK/send) with zero duplicated dedupe logic; replay is idempotent.
- A binding-symmetry regression test asserts the method EXISTS (not a stub) and exercises a real D1 task + Wire-contract + replay-no-op + null-return.
- INT-WARN-1 closed: the 5 Phase-1 wranglers carry the shared `database_id` so `wrangler deploy` binds the single atlas-db.
- No new npm packages; no Pillar violations (single writer / suggest-don't-destroy / D1 authoritative / idempotent).
</success_criteria>

<output>
Create `.planning/quick/260606-c24-close-milestone-v1-0-integration-gaps-im/260606-c24-SUMMARY.md` when done.
</output>
