---
phase: 04-outward-gated
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mcp-github/src/index.ts
  - apps/mcp-github/test/pr-tools.test.ts
autonomous: false
requirements: [OUTWARD-02]
user_setup:
  - service: github-app
    why: "Envoy's portfolio PR needs the GitHub App to carry pull_requests:write in addition to the existing contents:write"
    dashboard_config:
      - task: "Add the 'Pull requests: Read and write' permission to the Atlas GitHub App, then re-install on the scoped repos to apply the new permission"
        location: "GitHub → Settings → Developer settings → GitHub Apps → Atlas → Permissions & events"
must_haves:
  truths:
    - "mcp-github exposes github_create_branch and github_open_pr tools, each gated by hasScope('github.write') (403 on missing scope)"
    - "Both tools mint an installation token with permissions including pull_requests:'write'; the token is consumed server-side and NEVER returned in the tool result"
    - "github_create_branch reads the base branch SHA then creates refs/heads/<branch>; github_open_pr opens a non-draft PR and returns the PR html_url + number (no token)"
  artifacts:
    - path: "apps/mcp-github/src/index.ts"
      provides: "github_create_branch + github_open_pr registerTool entries"
      contains: "github_open_pr"
  key_links:
    - from: "apps/mcp-github/src/index.ts github_open_pr"
      to: "GitHub REST POST /repos/{owner}/{repo}/pulls"
      via: "installationToken with pull_requests:write"
      pattern: "pull_requests"
---

<objective>
Add two new tools to the existing `apps/mcp-github` MCP Worker — `github_create_branch` and `github_open_pr` — so Envoy (OUTWARD-02) can open its portfolio PR (D4-10: agent completes after confirm; a PR is draft-by-nature and reversible). Extends the existing GitHub App pattern (RS256 JWT → opaque `ghs_` installation token minted per-run) and the existing `github.write` scope check; no new scope is introduced.

Purpose: Envoy's GitHub targets (profile README commit + portfolio PR) are the reversible, authorized, no-browser path (D4-07/D4-10). The README commit reuses the existing `github_put_file`; the portfolio PR needs branch-create + open-PR, which do not exist yet.

Output: two new `registerTool()` entries in `apps/mcp-github/src/index.ts` + a test file.

NOTE (RESEARCH open question A1): the GitHub App permission string for PR creation is `pull_requests: "write"` ([ASSUMED]). The blocking human-verify checkpoint below confirms the App permission grant before relying on it live; the tool code + tests do not require the live grant (mocked GitHub responses).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-outward-gated/04-CONTEXT.md
@.planning/phases/04-outward-gated/04-RESEARCH.md
@.planning/phases/04-outward-gated/04-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing mcp-github contracts the new tools extend (read from the file before editing). -->
From apps/mcp-github/src/index.ts (existing):
- this.server.registerTool(name, { title, description, inputSchema? }, handler)
- this.hasScope("github.write"): boolean
- this.forbidden(): tool result (403-equivalent)
- this.mintToken({ permissions: Record<string,string> }): Promise<void>   // token stays server-side
- this.mintError(err): tool result (502)
- installationToken(env, installId, { permissions }) from ./github-app.ts
- GITHUB_SCOPES constant (line ~45)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add github_create_branch + github_open_pr tools (registerTool, hasScope, mintToken)</name>
  <read_first>
    - apps/mcp-github/src/index.ts (THE FILE BEING MODIFIED — read the existing github_put_file registerTool entry, hasScope/forbidden/mintError/mintToken private helpers, GITHUB_SCOPES constant, and the McpAgent class shape)
    - apps/mcp-github/src/github-app.ts (installationToken(env, installId, { permissions }) — how the App mints the per-run ghs_ token; what permissions object it accepts)
    - 04-PATTERNS.md "apps/mcp-github/src/index.ts — MODIFY" section (exact registerTool structure to copy, the REST call sequence, the permissions object to pass)
    - 04-RESEARCH.md Pattern 5 (github_create_branch/github_open_pr inputSchema + REST endpoints + the A1 [ASSUMED] permission note)
    - CLAUDE.md "Tech stack & PINNED versions" (agents ^0.14.x, @modelcontextprotocol/sdk 1.29.0 registerTool — verify against the installed .d.ts before writing, per the Phase-0 method)
  </read_first>
  <behavior>
    - github_create_branch(owner, repo, branch, fromBranch): with scope → GET /repos/{owner}/{repo}/git/ref/heads/{fromBranch} to read the base SHA, then POST /repos/{owner}/{repo}/git/refs with { ref: "refs/heads/{branch}", sha }; returns the created branch name in the tool result (no token). Without github.write scope → forbidden() (403).
    - github_open_pr(owner, repo, title, body, head, base): with scope → POST /repos/{owner}/{repo}/pulls with { title, body, head, base, draft: false }; returns { pr_url, pr_number } (no token). Without scope → forbidden().
    - Both mint the installation token with permissions { contents: "write", metadata: "read", pull_requests: "write" }; the minted token never appears in any tool result (existing T-00-32 invariant).
    - A GitHub API non-2xx → mintError-style error result (not a thrown unhandled rejection).
  </behavior>
  <action>
    In `apps/mcp-github/src/index.ts`, add two `this.server.registerTool(...)` entries immediately after the existing `github_put_file` entry, copying its exact structure (title/description/inputSchema, leading `if (!this.hasScope("github.write")) return this.forbidden();`, try/catch around `this.mintToken(...)` → `this.mintError(err)`). Both call `this.mintToken({ permissions: { contents: "write", metadata: "read", pull_requests: "write" } })`. `github_create_branch` performs the two-call REST sequence (read base ref SHA, create new ref); `github_open_pr` performs the single POST to /pulls with `draft: false` and returns the PR html_url + number. Reuse the existing `github.write` scope (do NOT introduce a new scope — per 04-PATTERNS the existing scope covers both). Keep the minted token strictly server-side; return only result data. If the project routes GitHub fetches through a helper in github-app.ts, reuse it; otherwise add a thin internal fetch keyed off the installation token consistent with how github_put_file issues its write. Create `apps/mcp-github/test/pr-tools.test.ts` exercising: scope-pass create-branch (mock GitHub 200 for ref read + ref create), scope-pass open-pr (mock 201 with html_url), scope-fail forbidden, and an assertion that no `ghs_` token string appears in any returned tool result.
  </action>
  <verify>
    <automated>pnpm test --filter mcp-github && pnpm --filter mcp-github typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test --filter mcp-github` green including the new `pr-tools.test.ts`.
    - `grep -c 'registerTool' apps/mcp-github/src/index.ts` increased by exactly 2 vs the pre-edit count; `github_create_branch` and `github_open_pr` both present.
    - Both new handlers begin with the `hasScope("github.write")` → `forbidden()` guard (source grep).
    - Both mintToken calls include `pull_requests: "write"` (source grep `pull_requests`).
    - Test asserts no returned tool result contains a substring matching `/ghs_[A-Za-z0-9]/` (token never egresses).
    - No new MCP scope string added to GITHUB_SCOPES beyond what already covers github.write (the existing scope is reused).
  </acceptance_criteria>
  <done>github_create_branch + github_open_pr registered, scope-gated, minting pull_requests:write, returning PR url/number with the token kept server-side; mocked tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Confirm GitHub App pull_requests:write permission grant (owner go-live)</name>
  <action>This is an owner-only go-live checkpoint — no code change. The owner adds the "Pull requests: Read and write" permission to the Atlas GitHub App and re-installs it on the scoped repos so github_open_pr can mint a token with pull_requests:write. Pause execution and wait for the owner per how-to-verify; if GitHub names the permission key something other than `pull_requests`, correct the mintToken permissions object in apps/mcp-github accordingly before resuming.</action>
  <what-built>The github_open_pr tool mints an installation token requesting pull_requests:"write". The Atlas GitHub App currently grants only contents:write + metadata:read (Phase 0). Code + mocked tests are complete and green; this checkpoint confirms the live App permission grant before Envoy's portfolio PR can fire end-to-end. [ASSUMED] A1: the permission string is pull_requests:"write".</what-built>
  <how-to-verify>
    1. Go to GitHub → Settings → Developer settings → GitHub Apps → Atlas → Permissions & events.
    2. Confirm "Pull requests" is set to "Read and write". If not, set it.
    3. Verify the exact permission key name GitHub uses matches `pull_requests` (the REST installation-token permissions key). If GitHub names it differently, report the exact string so the mintToken permissions object can be corrected.
    4. Re-install / accept the updated permissions on the scoped portfolio + profile repos (GitHub requires re-acceptance when permissions change).
    5. Confirm `GH_APP_INSTALLATION_ID` is still valid (the App did not need re-registration, only a permission update).
  </how-to-verify>
  <resume-signal>Type "approved" (and the exact permission key string if it is not `pull_requests`), or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (Envoy) → mcp-github tool | scope-gated; only github.write callers may create branches / open PRs |
| mcp-github → GitHub REST API | App installation token (ghs_) minted per-run, must never leak back to the MCP client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Elevation of Privilege | a non-github.write caller creating branches/PRs | mitigate | `hasScope("github.write")` guard at the top of both handlers → forbidden() (Task 1) |
| T-04-09 | Information Disclosure | installation token (ghs_) leaking into tool result | mitigate | mintToken result consumed server-side only; test asserts no /ghs_/ substring in any result (existing T-00-32 invariant carried) |
| T-04-10 | Spoofing | wrong/over-broad App permission scope | mitigate | mint exactly { contents:write, metadata:read, pull_requests:write }; least-privilege; blocking human-verify confirms the App grants only these |
| T-04-SC | Tampering | new package install | accept | No new package — extends the existing mcp-github (agents ^0.14.x + @modelcontextprotocol/sdk 1.29.0 already pinned + audited [OK]) |
</threat_model>

<verification>
- `pnpm test --filter mcp-github` green; `pnpm -r typecheck` green.
- Two new registerTool entries; both scope-gated; both mint pull_requests:write; token never returned.
- guard-wire-consumer.js unaffected (mcp-github is not a Wire consumer).
</verification>

<success_criteria>
- github_create_branch + github_open_pr exist, scope-gated, minting pull_requests:write, returning PR url/number.
- The GitHub App pull_requests:write grant is confirmed by the owner (blocking checkpoint) before Envoy relies on it live.
</success_criteria>

<output>
Create `.planning/phases/04-outward-gated/04-02-SUMMARY.md` when done.
</output>
