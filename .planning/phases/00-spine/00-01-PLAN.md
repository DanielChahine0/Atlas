---
phase: 00-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - vitest.workspace.ts
  - .gitignore
  - apps/atlas/wrangler.jsonc
  - apps/atlas/src/index.ts
  - apps/atlas/vitest.config.ts
  - apps/steward/wrangler.jsonc
  - migrations/0001_init_core.sql
  - docs/03-scheduling.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SPINE-01, SPINE-02, SPINE-05]
must_haves:
  truths:
    - "The pnpm monorepo builds: pnpm -r build and pnpm -r typecheck pass; pnpm test runs Vitest inside workerd with TZ=UTC"
    - "D1 atlas-db exists with the five system-of-record tables (idempotency_keys, counters, run_log, audit_log, vault_outbox)"
    - "The atlas-wire Queue and atlas-wire-dlq dead-letter Queue both exist; a Worker can env.WIRE.send() the canonical §6.4 event shape"
    - "CONFIG KV, OAUTH_KV KV, and atlas-blobs R2 (with audio/raw/ 7-day lifecycle) exist"
    - "docs/03-scheduling.md contains the EST/EDT UTC cron translation table; PROJECT.md AND ROADMAP.md no longer assert a Paid hard prerequisite (Free is sufficient per D-01/D-02)"
    - "Implements locked CONTEXT.md decisions D-04 (pnpm workspaces), D-05 (one Worker per agent — apps/<codename> isolation), D-06 (UTC crons + twice-yearly EST/EDT hand-edit policy), D-07 (EST/EDT table written to docs/03-scheduling.md), and D-09 (compatibility_date 2026-04-25 + nodejs_compat)"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace roots [apps/*, packages/*]"
      contains: "apps/*"
    - path: "migrations/0001_init_core.sql"
      provides: "D1 system-of-record schema (5 tables, reconciled run_log superset)"
      contains: "CREATE TABLE IF NOT EXISTS vault_outbox"
    - path: "apps/atlas/wrangler.jsonc"
      provides: "Atlas Worker config with all canonical bindings"
      contains: "atlas-wire"
    - path: "tsconfig.base.json"
      provides: "strict TS base config"
      contains: "\"strict\": true"
  key_links:
    - from: "apps/atlas/wrangler.jsonc"
      to: "atlas-wire"
      via: "queues.producers binding WIRE"
      pattern: "\"binding\": *\"WIRE\""
    - from: "apps/atlas/wrangler.jsonc"
      to: "atlas-db"
      via: "d1_databases binding DB + migrations_dir"
      pattern: "\"binding\": *\"DB\""
---

<objective>
Scaffold the greenfield pnpm monorepo and provision every Cloudflare resource the spine sits on: D1 (system-of-record), the Wire (Queue + DLQ), KV (CONFIG + OAUTH_KV), R2 (atlas-blobs), plus the root toolchain (strict TS, Vitest-in-workerd) and the two wrangler.jsonc configs that declare the canonical bindings. Also reconcile the stale "Paid required" claims (D-02) and write the EST/EDT cron table (D-07).

Purpose: Every later wave imports these resources by their canonical binding names. Getting the schema, the binding strings, and the provisioning right here means the 16 agents inherit them for free. This is the foundation Wave 2 (Atlas/Steward DOs) and Wave 3 (OAuth/bridge) build on.
Output: A buildable monorepo, a migrated D1 with five tables, a live Wire + DLQ, KV/R2 namespaces, and two wrangler.jsonc files declaring the full canonical binding set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/00-spine/00-CONTEXT.md
@.planning/phases/00-spine/00-RESEARCH.md
@.planning/phases/00-spine/00-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-T0 cleanup + pnpm monorepo scaffold + root toolchain</name>
  <files>pnpm-workspace.yaml, package.json, tsconfig.base.json, vitest.workspace.ts, .gitignore, apps/atlas/wrangler.jsonc, apps/atlas/src/index.ts, apps/atlas/vitest.config.ts</files>
  <read_first>
    - CLAUDE.md (the "Repo layout" + "Tech stack & PINNED versions" + "Canonical conventions" sections — the EXACT binding/version strings)
    - docs/13-build-plan.md §2 "Repo & code layout" (lines 54-93) + §3 "Tooling baseline" (lines 97-130) — the layout tree and the C3 scaffold/install commands
    - .planning/phases/00-spine/00-RESEARCH.md "Recommended Project Structure" + "Installation" + "Runtime State Inventory" (the slopcheck side-effect cleanup note) + "Validation Architecture" (the vitest.config.ts pool config block)
    - .planning/phases/00-spine/00-PATTERNS.md "apps/atlas/wrangler.jsonc" section + "satisfies ExportedHandler<Env>" shared pattern
    - .gitignore (current state — confirm node_modules/, .dev.vars, secrets globs already present)
  </read_first>
  <action>
    First delete the slopcheck side-effect: `rm -rf node_modules package.json package-lock.json` at repo root (RESEARCH Runtime State Inventory — these are NOT the intended scaffold; node_modules is untracked so this is safe). Then scaffold the pnpm monorepo per D-04: create `pnpm-workspace.yaml` with `packages: ["apps/*", "packages/*"]`; a root `package.json` with `"packageManager": "pnpm@..."`, `"private": true`, and root scripts `build`/`test`/`typecheck`/`lint`/`format` that run `pnpm -r` across workspaces; `tsconfig.base.json` with `"strict": true` and `compilerOptions` shared by all Workers (per build-plan §3). Scaffold the first Worker via `npm create cloudflare@latest apps/atlas -- --type=hello-world --ts` (this writes wrangler.jsonc + TS — C3 default), then add deps at root: `pnpm add -D -w vitest @cloudflare/vitest-pool-workers wrangler typescript eslint @typescript-eslint/eslint-plugin @typescript-eslint/parser prettier` and `pnpm add -w @anthropic-ai/sdk agents @modelcontextprotocol/sdk@1.29.0 @cloudflare/workers-oauth-provider zod jose`. Re-verify the `agents` pin is still `^0.14.x` before installing (it ships ~weekly; RESEARCH valid-until 2026-06-18) using `/cf-docs` or the cloudflare-researcher subagent if unsure. Create `vitest.workspace.ts` at root that GLOBS `["apps/*/vitest.config.ts", "packages/*/vitest.config.ts"]` (NOT an explicit per-app list — every test-bearing app/package owns its own vitest.config.ts and the glob picks them up automatically), and `apps/atlas/vitest.config.ts` using `defineWorkersConfig` from `@cloudflare/vitest-pool-workers/config` with `wrangler.configPath: "./wrangler.jsonc"`, `miniflare.compatibilityFlags: ["nodejs_compat"]`, `isolatedStorage: true` (RESEARCH Validation Architecture block). Write `apps/atlas/src/index.ts` as a minimal default export using `satisfies ExportedHandler<Env>` (NOT the `: ExportedHandler<Env>` annotation) — a hello-world fetch handler is fine for this task; the scheduled() dispatcher + OAuthProvider come in Wave 2. Ensure `.gitignore` keeps node_modules/, .dev.vars*, and the secret globs (already present — verify, do not weaken).
  </action>
  <verify>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && pnpm -r build && pnpm -r typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rm -rf node_modules package.json package-lock.json` ran before scaffold (the stray slopcheck artifacts are gone; the real pnpm scaffold replaces them)
    - `pnpm-workspace.yaml` contains both `apps/*` and `packages/*`
    - `pnpm -r build` and `pnpm -r typecheck` both exit 0
    - `tsconfig.base.json` contains `"strict": true`
    - `apps/atlas/src/index.ts` uses `satisfies ExportedHandler<Env>` (grep: `grep -c 'satisfies ExportedHandler<Env>' apps/atlas/src/index.ts` >= 1) and does NOT use the `: ExportedHandler<Env>` annotation
    - `apps/atlas/vitest.config.ts` imports `defineWorkersConfig` and sets `compatibilityFlags: ["nodejs_compat"]` and `isolatedStorage: true`
    - `vitest.workspace.ts` GLOBS `["apps/*/vitest.config.ts", "packages/*/vitest.config.ts"]` (not an explicit per-app list): `grep -q 'apps/\*/vitest.config.ts' vitest.workspace.ts && grep -q 'packages/\*/vitest.config.ts' vitest.workspace.ts`
    - `package.json` declares pnpm as packageManager and `"private": true`
  </acceptance_criteria>
  <done>The monorepo scaffolds, builds, and typechecks; the slopcheck stray artifacts are removed; vitest pool config is in place with nodejs_compat.</done>
</task>

<task type="auto">
  <name>Task 2: D1 migration 0001_init_core + provision D1/KV/R2/Queues + Atlas wrangler.jsonc bindings + Steward wrangler.jsonc skeleton</name>
  <files>migrations/0001_init_core.sql, apps/atlas/wrangler.jsonc, apps/steward/wrangler.jsonc</files>
  <read_first>
    - docs/13-build-plan.md T1 (lines 371-413, the D1 schema + commands), T2 (lines 417-429, KV+R2), T3 (lines 433-451, Wire+DLQ) — the provisioning commands and acceptance
    - docs/13-build-plan.md §5.2 "Observability" reconciled run_log schema (lines 1242-1256) — the richer superset with duration_ms/rows_read/rows_written
    - .planning/phases/00-spine/00-RESEARCH.md "D1 init migration (T1)" code block (lines 437-454, the reconciled five-table schema with audit_log ULID PK + scope_used) + the run_log reconciliation note
    - .planning/phases/00-spine/00-PATTERNS.md "migrations/0001_init_core.sql" section + "apps/atlas/wrangler.jsonc" section (the full exact-strings list) + "apps/steward/wrangler.jsonc" section
    - CLAUDE.md "Canonical conventions" binding table + "Gotchas" (D1 positional ? only; KV not for counters; new_sqlite_classes)
  </read_first>
  <action>
    Write `migrations/0001_init_core.sql` with the FIVE reconciled tables (absolute increment math; positional `?` binding is enforced at the call site, not the DDL): `idempotency_keys` (PK `key`, plus `agent NOT NULL`, `type`, `entity`, `op`, `applied_at INTEGER NOT NULL`); `counters` (PK `entity`, `value INTEGER NOT NULL DEFAULT 0`); `run_log` — use the §5.2 RICHER superset: `id INTEGER PRIMARY KEY AUTOINCREMENT`, `agent`, `type`, `entity`, `op`, `rows_read INTEGER`, `rows_written INTEGER`, `duration_ms INTEGER`, `result TEXT`, `ts INTEGER NOT NULL` (RESEARCH A4 — Flagger reads this in Phase 2); `audit_log` (TEXT/ULID PK `id`, `ts`, `agent`, `action`, `target`, `scope_used` — records scope NEVER the token, `gated INTEGER`, `decision`, `outcome`, `trust INTEGER`, `consent_flag INTEGER`, `flag_id`); `vault_outbox` (PK `idem`, `path NOT NULL`, `method NOT NULL`, `headers NOT NULL`, `body`, `state NOT NULL DEFAULT 'pending'`, `ts NOT NULL`). Name the audit table `audit_log` (not `audit`) to match the §5.2 superset and the security invariant references. Provision the resources with wrangler (Free plan per D-01): `wrangler d1 create atlas-db`; `wrangler d1 migrations apply atlas-db --local` then `--remote`; `wrangler kv namespace create CONFIG`; `wrangler kv namespace create OAUTH_KV`; `wrangler r2 bucket create atlas-blobs`; `wrangler r2 bucket lifecycle add atlas-blobs --name expire-raw-audio --prefix "audio/raw/" --expire-days 7`; `wrangler queues create atlas-wire`; `wrangler queues create atlas-wire-dlq`. Then write `apps/atlas/wrangler.jsonc` with the FULL canonical binding set per PATTERNS exact-strings list: `"$schema": "./node_modules/wrangler/config-schema.json"`, `"name": "atlas"`, `"main": "src/index.ts"`, `"compatibility_date": "2026-04-25"` (D-09 — NOT the build-plan's 2026-04-07 floor), `"compatibility_flags": ["nodejs_compat"]`; DO bindings `{name:"ATLAS",class_name:"AtlasCoordinator"}` and `{name:"MORNING_CHAIN_DO",class_name:"AtlasCoordinator"}`; migration `{tag:"v1", new_sqlite_classes:["AtlasCoordinator"]}` (Atlas owns its class; Steward owns StewardWriter in its own config); `queues.producers` `{binding:"WIRE", queue:"atlas-wire"}` (Atlas is a PRODUCER, never a consumer); `d1_databases` `{binding:"DB", database_name:"atlas-db", migrations_dir:"migrations"}`; `kv_namespaces` for `CONFIG` and `OAUTH_KV`; `r2_buckets` `{binding:"BLOBS", bucket_name:"atlas-blobs"}`; `ai` `{binding:"AI"}`; `[vars]` with plaintext-only `AIG_ACCOUNT_ID`/`AIG_GATEWAY_ID`/`MODEL_*` placeholders (NEVER secrets/counters); a staging override setting `env.staging.triggers.crons = []` (staging fires NO crons). Do NOT add `triggers.crons` lines yet — cron lines first fire in Phase 1. Write `apps/steward/wrangler.jsonc` skeleton with `"name": "steward"`, the same compatibility_date/flags, DO binding `{name:"STEWARD_LOCK", class_name:"StewardWriter"}`, migration `{tag:"v1", new_sqlite_classes:["StewardWriter"]}`, the `DB`/`CONFIG` bindings it needs, and a producer `WIRE` binding (Steward emits Flagger events) — but leave the `queues.consumers` block to be added in Wave 2 Plan 04 (the consumer is Steward's alone and lands with its handler).
  </action>
  <verify>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && wrangler d1 migrations apply atlas-db --local && wrangler d1 execute atlas-db --local --command "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name"</automated>
  </verify>
  <acceptance_criteria>
    - `wrangler d1 execute atlas-db --local --command "SELECT name FROM sqlite_master WHERE type='table'"` lists all five: `idempotency_keys`, `counters`, `run_log`, `audit_log`, `vault_outbox`
    - `run_log` has `duration_ms`, `rows_read`, `rows_written` columns (the §5.2 superset, not the slimmer T1 shape): `wrangler d1 execute atlas-db --local --command "PRAGMA table_info(run_log)"` shows duration_ms
    - `audit_log` has a `scope_used` column and NO `token`/`access_token` column (the security invariant — records scope, never the token)
    - `apps/atlas/wrangler.jsonc` contains `"compatibility_date": "2026-04-25"` and `"compatibility_flags": ["nodejs_compat"]` and `new_sqlite_classes` (NOT `new_classes`)
    - `apps/atlas/wrangler.jsonc` declares producer `WIRE` on `atlas-wire` and has NO `queues.consumers` block (Atlas is never a consumer): `grep -c 'consumers' apps/atlas/wrangler.jsonc` == 0
    - `apps/atlas/wrangler.jsonc` `env.staging.triggers.crons` is `[]`
    - No secret values appear in either wrangler.jsonc `[vars]` block (only AIG_ACCOUNT_ID/AIG_GATEWAY_ID/MODEL_* placeholders)
    - `wrangler queues list` shows both `atlas-wire` and `atlas-wire-dlq`
  </acceptance_criteria>
  <done>D1 migrated with the five reconciled tables; Wire+DLQ+KV+R2 provisioned on Free; both wrangler.jsonc files declare the exact canonical bindings with the D-09 compatibility_date.</done>
</task>

<task type="auto">
  <name>Task 3: Doc reconciliation — EST/EDT cron table (D-07) + drop stale "Paid required" (D-02)</name>
  <files>docs/03-scheduling.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
    - docs/03-scheduling.md (current state — find the §6 timezone open-question region the table replaces)
    - docs/13-build-plan.md §1 "Decision the owner makes here: the cron timezone policy" (line 50) + setup acceptance #4 (line 308) — the policy to document
    - .planning/PROJECT.md (lines ~43, ~58 — the stale "Paid hard prerequisite" assertions D-02 says to reconcile)
    - .planning/ROADMAP.md (Phase 0 Details "**Depends on**" line — the stale "Hard prerequisite: Cloudflare Workers **Paid** plan" assertion D-02 also covers)
    - .planning/phases/00-spine/00-CONTEXT.md D-01/D-02/D-06/D-07
    - CLAUDE.md "Gotchas" cron section (07:45 ET = `45 12 * * *` EST / `45 11 * * *` EDT)
  </read_first>
  <action>
    Add the EST/EDT UTC cron translation table to `docs/03-scheduling.md` next to the cron policy discussion, documenting D-06: Cloudflare Cron Triggers are UTC-only with no DST; the policy is twice-yearly hand-edit at the EST↔EDT boundary. The table maps each owner-local morning-chain target to both its EST and EDT UTC cron expression — at minimum the Filer sweep `07:45 ET = "45 12 * * *"` (EST) / `"45 11 * * *"` (EDT); include the other morning-chain target times (Herald 08:00, Forge 08:15, Sundial 08:20, Compass 08:30, plus the 21:00 Compass preview) so Phase 1 has the full table ready. Note that in-Workflow waits use `step.sleepUntil` with `America/Toronto` Intl (DST-safe). In `.planning/PROJECT.md`, edit lines ~43 and ~58 (and any other "Workers Paid required / hard prerequisite" assertion) to state that the Workers FREE plan is sufficient for the spine per D-01 (Queues GA-on-Free 2026-02-04, Workflows on Free, SQLite DOs on Free), with Workers Paid being an OPTIONAL headroom upgrade only (relevant later for a confirm-gate waiting >3 days — a Phase-4 concern). Apply the SAME one-line reconciliation to `.planning/ROADMAP.md`: in the Phase 0 Details section the `**Depends on**` line still asserts "Hard prerequisite: Cloudflare Workers **Paid** plan (Queues, Workflows, KV-backed DOs)" — change it to state the spine builds & deploys on the Workers **Free** plan per D-01 (Paid is optional headroom only — e.g. a confirm-gate waiting >3 days, a Phase-4 concern); touch ONLY that prose line, never the GSD-managed plan checklist or wave annotations. Keep the edits minimal and surgical — do not rewrite surrounding prose. (GSD owns .planning/ STATE/counters, but PROJECT.md/ROADMAP.md content reconciliation per a locked decision D-02 is the intended action here.)
  </action>
  <verify>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && grep -q "45 12" docs/03-scheduling.md && grep -q "45 11" docs/03-scheduling.md && ! grep -iE "paid.{0,40}(hard|required|prerequisite)" .planning/PROJECT.md && ! grep -iE "paid.{0,40}(hard|required|prerequisite)" .planning/ROADMAP.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/03-scheduling.md` contains both `45 12 * * *` (EST) and `45 11 * * *` (EDT) cron expressions in a labeled EST/EDT translation table
    - `docs/03-scheduling.md` references `step.sleepUntil` / `America/Toronto` for DST-safe in-Workflow waits
    - `.planning/PROJECT.md` no longer asserts Workers Paid as a hard prerequisite/requirement (grep -iE "paid.{0,40}(hard|required|prerequisite)" returns nothing)
    - `.planning/PROJECT.md` states the Free plan is sufficient for the spine (D-01)
    - `.planning/ROADMAP.md` Phase 0 "Depends on" no longer lists Workers Paid as a hard prerequisite (`grep -iE "paid.{0,40}(hard|required|prerequisite)" .planning/ROADMAP.md` returns nothing) and states the Free plan is sufficient (D-01/D-02)
  </acceptance_criteria>
  <done>The EST/EDT cron table is documented (D-07 setup-done criterion); the stale Paid-prerequisite claims are reconciled to Free-is-sufficient (D-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> Cloudflare account | wrangler provisioning commands create account-scoped resources (D1, Queues, KV, R2) |
| tracked git files -> repo | wrangler.jsonc / migrations / docs are committed; must never carry secrets |
| npm registry -> repo | dependency install pulls third-party packages (supply-chain surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Information Disclosure | wrangler.jsonc `[vars]` | mitigate | `[vars]` holds plaintext non-secrets ONLY (AIG_ACCOUNT_ID/AIG_GATEWAY_ID/MODEL_*); secrets land in Secrets Store in Plan 06/07 — acceptance asserts no secret in either `[vars]` block |
| T-00-02 | Tampering | D1 counters/idempotency | mitigate | counters + idempotency_keys live in D1 (never KV — KV is 1 write/s/key + lag); schema enforces PK uniqueness for replay dedup downstream |
| T-00-03 | Information Disclosure | audit_log schema | mitigate | audit_log records `scope_used` and has NO token column by design; acceptance asserts the column set |
| T-00-04 | Tampering (supply chain) | npm install (`agents` etc.) | mitigate | install via pnpm/npm explicitly (RESEARCH cross-ecosystem finding: PyPI `agents` is a different package); pins from CLAUDE.md re-verified; all packages [OK] per Package Legitimacy Audit |
| T-00-05 | Repudiation | missing DLQ | mitigate | `atlas-wire-dlq` is provisioned here (SPINE-05 foundation); its consumer + Flagger sink land in Plan 04 |
| T-00-06 | Information Disclosure | committed secret globs | accept | `.gitignore` already blocks .dev.vars*, *.pem, *.key, google-*.json, github-app-*.private-key.pem — verified, not weakened |
| T-00-SC | Tampering | npm/pnpm installs | mitigate | RESEARCH Package Legitimacy Audit (npm ecosystem) returned all [OK]; `vitest` [SUS] is a confirmed typosquat false-positive (no checkpoint needed); no [ASSUMED]/[SLOP] packages |
</threat_model>

<verification>
- `pnpm -r build && pnpm -r typecheck` pass (monorepo is buildable)
- `wrangler d1 execute atlas-db --local --command "SELECT name FROM sqlite_master WHERE type='table'"` lists all five tables
- `wrangler queues list` shows `atlas-wire` and `atlas-wire-dlq`
- `grep -c 'consumers' apps/atlas/wrangler.jsonc` == 0 (Atlas is never a Wire consumer — Pillar 1)
- No secret values in any `[vars]` block (security invariant)
</verification>

<success_criteria>
- Monorepo builds + typechecks; slopcheck stray artifacts removed
- D1 atlas-db migrated with the five reconciled tables (run_log = §5.2 superset; audit_log records scope_used not token)
- atlas-wire + atlas-wire-dlq + CONFIG + OAUTH_KV + atlas-blobs (audio/raw/ 7d lifecycle) all provisioned on Free
- Atlas + Steward wrangler.jsonc declare the exact canonical bindings; compatibility_date = 2026-04-25 + nodejs_compat
- EST/EDT cron table written (D-07); Paid-prerequisite claims reconciled (D-02)
</success_criteria>

<output>
Create `.planning/phases/00-spine/00-01-SUMMARY.md` when done
</output>
