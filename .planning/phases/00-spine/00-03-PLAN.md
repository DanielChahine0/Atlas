---
phase: 00-spine
plan: 03
type: execute
wave: 3
depends_on: [00-02]
files_modified:
  - apps/atlas/src/index.ts
  - apps/atlas/src/coordinator.ts
  - apps/atlas/src/noop-agent.ts
  - apps/atlas/test/scheduled.test.ts
  - apps/atlas/test/heartbeat.test.ts
autonomous: true
requirements: [SPINE-01]
must_haves:
  truths:
    - "Atlas can schedule a no-op agent and route a message onto the Wire (ROADMAP Phase-0 Success Criterion 1): a cron tick reaches the scheduled() dispatcher, which invokes the no-op agent via service-binding RPC (D-11) and then calls env.WIRE.send() with the canonical 6.4 event shape"
    - "The AtlasCoordinator DO is addressed only as env.ATLAS.getByName(root) - two getByName(root) calls resolve to one instance (one name = one lock)"
    - "Atlas self-monitors its own liveness: the DO alarm() fires on a 1-minute cadence and, when no heartbeat has landed within the 5-minute staleness window (D-10), emits a P1 incident onto the Wire toward Flagger"
    - "The dispatcher emitted event carries a STABLE, structured, date-derived idempotencyKey (atlas:noop:<owner-local-date>) - never crypto.randomUUID() - so a re-fired or missed-then-recovered cron replays as a no-op downstream"
    - "Atlas is a Wire PRODUCER only: it never declares a queues.consumers block and never writes the Vault/Gmail/Codex (Pillar 1 - Steward is the sole atlas-wire consumer)"
  artifacts:
    - path: "apps/atlas/src/coordinator.ts"
      provides: "AtlasCoordinator Durable Object: 1-min alarm() heartbeat self-monitor (5-min staleness -> P1 to Flagger via the Wire, D-10); beat()/startHeartbeat() methods; addressed as getByName(root)"
      contains: "class AtlasCoordinator"
    - path: "apps/atlas/src/noop-agent.ts"
      provides: "The no-op agent service-binding RPC target (D-11) Atlas invokes before routing onto the Wire - a WorkerEntrypoint/handler with a single tick() RPC method; no domain work"
      contains: "noop"
    - path: "apps/atlas/src/index.ts"
      provides: "Atlas default export with the scheduled() dispatcher: switch(controller.cron) -> invoke the no-op agent via service-binding RPC -> env.WIRE.send(canonicalEvent); uses satisfies ExportedHandler<Env>; re-exports the AtlasCoordinator DO class"
      contains: "scheduled"
    - path: "apps/atlas/test/scheduled.test.ts"
      provides: "SPINE-01 integration test driving the scheduled() handler and asserting a 6.4-shaped event reaches the Wire with a structured idempotencyKey (Wire-contract test)"
      contains: "scheduled"
    - path: "apps/atlas/test/heartbeat.test.ts"
      provides: "Heartbeat tests: a fresh beat() leaves the DO healthy; a >5-min-stale lastBeat triggers a P1 emit; two getByName(root) resolve to one DO (failure-path / single-instance test)"
      contains: "getByName"
  key_links:
    - from: "apps/atlas/src/index.ts"
      to: "env.WIRE (atlas-wire Queue producer)"
      via: "env.WIRE.send(canonicalEvent) inside scheduled()"
      pattern: "WIRE\\.send"
    - from: "apps/atlas/src/index.ts"
      to: "the no-op agent (D-11 service-binding RPC)"
      via: "service-binding RPC invocation before WIRE.send"
      pattern: "noop|tick"
    - from: "apps/atlas/src/index.ts"
      to: "packages/wire (the single 6.4 WireEvent contract)"
      via: "import the WireEvent type / send helper from the 00-02 wire package"
      pattern: "wire"
    - from: "apps/atlas/src/coordinator.ts"
      to: "Flagger (P1 staleness incident)"
      via: "env.WIRE.send of a P1 incident event in alarm() when stale"
      pattern: "WIRE\\.send"
---

<objective>
Implement the Atlas orchestrator runtime: the `AtlasCoordinator` Durable Object plus the `scheduled()` dispatcher that proves SPINE-01 end-to-end - a cron tick routes a message onto the Wire after invoking a no-op agent over a service-binding RPC (D-11). This is the first behavior Phase 0 makes observable: schedule -> invoke -> `WIRE.send`. It also stands up Atlas self-supervision: a 5-minute heartbeat staleness self-flag to Flagger (D-10).

Purpose: Atlas is the orchestrator that every later agent depends on for scheduling and routing. SPINE-01 ("Atlas can schedule a no-op agent and route a message onto the Wire") is ROADMAP Phase-0 Success Criterion 1. Getting the dispatcher contract, the canonical DO name (`getByName("root")`), the structured idempotency key, the producer-only posture (Pillar 1), and the heartbeat right here means the Phase-1 morning chain inherits a working orchestrator skeleton for free.

Output: `apps/atlas/src/coordinator.ts` (the `AtlasCoordinator` DO with the heartbeat alarm), `apps/atlas/src/noop-agent.ts` (the service-binding RPC target), a rewritten `apps/atlas/src/index.ts` (the `scheduled()` dispatcher replacing the Wave-1 hello-world; the OAuthProvider default-export composition is deliberately NOT here - it lands in 00-06), and two tests (`scheduled.test.ts`, `heartbeat.test.ts`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/00-spine/00-CONTEXT.md
@.planning/phases/00-spine/00-RESEARCH.md
@.planning/phases/00-spine/00-PATTERNS.md
@.planning/phases/00-spine/00-01-PLAN.md
@docs/agents/atlas.md

<interfaces>
Contracts this plan consumes from Wave-1 (00-01) and Wave-2 (00-02). Use these directly - do not re-derive from the codebase.

From packages/wire (00-02 - the single 6.4 WireEvent contract; import, never re-define):
  export const WireEvent  // zod schema for { agent, type, entity, op, payload, idempotencyKey }
  export type WireEvent = { agent: string; type: string; entity: string;
                            op: "increment" | "upsert" | "append";
                            payload: Record<string, unknown>; idempotencyKey: string }
  export function send(env, evt: WireEvent): Promise<void>   // producer helper (env.WIRE.send under the hood)
  NOTE: the 00-02 package name + exact send() signature/export names are defined by 00-02 package.json + src/index.ts -
  confirm the published exports from packages/wire/src/index.ts before importing (use send() if present, else env.WIRE.send directly).

From packages/shared (00-02):
  export interface Env { ... }   // canonical bindings: ATLAS (DO), WIRE (Queue producer), DB (D1), CONFIG (KV), AI, plus a no-op service binding
  export function flag(env: Env, severity: "P1"|"P2"|"P3"|"P4", reason: string, detail?: unknown): Promise<void>
  NOTE: flag() emits a Flagger incident onto the Wire (op:"append"); confirm its exact signature from packages/shared/src/flag.ts.

From apps/atlas/wrangler.jsonc (00-01 - already declares these bindings; do NOT edit this file in this plan):
  DO binding   { "name": "ATLAS", "class_name": "AtlasCoordinator" }   (+ MORNING_CHAIN_DO -> AtlasCoordinator)
  migration    { "tag": "v1", "new_sqlite_classes": ["AtlasCoordinator"] }
  producer     { "binding": "WIRE", "queue": "atlas-wire" }            (Atlas is a producer; NO consumers block)
  NOTE: the AtlasCoordinator class declared here MUST be exported from apps/atlas/src/index.ts (the main entry) so the migration resolves.
  The no-op service-binding (D-11) target: confirm the `services` binding name in wrangler.jsonc; if absent, record a wiring follow-up (Task 2). NO consumers block may be added anywhere.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the agents@0.14.1 Durable Object base-class API, then implement the AtlasCoordinator DO + heartbeat (D-10)</name>
  <files>apps/atlas/src/coordinator.ts</files>
  <read_first>
    - .planning/phases/00-spine/00-PATTERNS.md "apps/atlas/src/coordinator.ts (provider/DO)" section (lines 104-126 - the build-plan T4 AtlasCoordinator excerpt to replicate verbatim) AND "Planner Notes" item 4 (line 356 - the agents@0.14.1 class-API confirmation requirement)
    - .planning/phases/00-spine/00-RESEARCH.md "Atlas heartbeat self-monitor (D-10, P1 on stale)" code block (lines 397-414) + "Open Questions" #1 (lines 493-496 - the agents@0.14.1 DO/Workflow base-class is the one LOW-confidence area) + Assumption A3 (line 487)
    - docs/agents/atlas.md (the orchestrator contract - Atlas does NO domain work: schedules/routes/supervises only; never writes Vault/Gmail/Codex; supervises + escalates to Flagger) and CLAUDE.md "Canonical conventions" (DO class = PascalCase AtlasCoordinator; address only as env.ATLAS.getByName("root")) + CLAUDE.md "Gotchas"/D-10 (TZ=UTC everywhere; 5-min staleness -> P1)
    - apps/atlas/wrangler.jsonc (from 00-01 - the DO binding ATLAS -> AtlasCoordinator, the new_sqlite_classes:["AtlasCoordinator"] migration; do NOT edit this file)
    - packages/shared/src/env.ts + packages/shared/src/flag.ts (from 00-02 - the Env type and the flag() Flagger-emit helper this DO uses; OR emit the P1 directly via env.WIRE.send if flag() is producer-context-only - confirm from 00-02 exports)
  </read_first>
  <action>
    FIRST (RESEARCH correction 4 / Open Question 1 - the agents@0.14.1 DO base-class API is the single LOW-confidence area; do NOT copy possibly-stale class syntax): confirm the current DurableObject<Env> import path and method surface for agents@0.14.1 by invoking the project cloudflare-researcher subagent (or run /cf-docs durable objects). Specifically confirm: the DurableObject<Env> base-class import (the build-plan working hypothesis is cloudflare:workers), the constructor ctx/state parameter name and the ctx.storage API (put/get/getAlarm/setAlarm), and the alarm() handler signature. Record the confirmed import + signatures in the SUMMARY "API confirmation" note.

    THEN implement class AtlasCoordinator extends DurableObject<Env> per the PATTERNS lines 104-126 / RESEARCH lines 397-414 excerpt, using ONLY the confirmed API. beat(): persist lastBeat = Date.now() via ctx.storage.put("lastBeat", ...). startHeartbeat(): if ctx.storage.getAlarm() is null, schedule the first alarm at Date.now() + 60_000 (1-minute cadence). alarm(): read lastBeat (default 0); if Date.now() - lastBeat > 5 * 60_000 (D-10: the 5-minute staleness threshold), emit a P1 incident toward Flagger ON THE WIRE - env.WIRE.send with agent:"Atlas", type:"heartbeat.stale", entity:"atlas", op:"append", payload:{ severity:"P1", reason:"orchestrator heartbeat stale" }, idempotencyKey:"atlas:heartbeat-stale:<owner-local-date>" (or via the 00-02 flag(env,"P1",...) helper if it is callable from DO context). Then ALWAYS reschedule the next alarm at Date.now() + 60_000 INSIDE alarm() (one alarm per DO - setting a new time overwrites; never let the heartbeat stop). Derive <owner-local-date> with new Intl.DateTimeFormat('en-CA',{timeZone:'America/Toronto'}).format(new Date()) -> YYYY-MM-DD (CLAUDE.md gotcha: workerd/vitest force TZ=UTC, so new Date() is UTC even on the laptop - never compute the date naively). Use the 6.4 WireEvent shape/type imported from the 00-02 wire package - do NOT re-declare the event shape. Atlas is producer-only: this DO emits onto the Wire and reads its own storage; it NEVER writes the Vault/Gmail/Codex and NEVER consumes atlas-wire (Pillar 1). Type the DO against the Env from packages/shared.
  </action>
  <verify>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && pnpm --filter atlas typecheck && grep -q 'class AtlasCoordinator' apps/atlas/src/coordinator.ts && grep -Eq '5 ?[*] ?60' apps/atlas/src/coordinator.ts && grep -q 'America/Toronto' apps/atlas/src/coordinator.ts && [ "$(grep -cE 'consumers|async queue' apps/atlas/src/coordinator.ts)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - The agents@0.14.1 DurableObject<Env> import + ctx.storage/alarm() surface was confirmed via the cloudflare-researcher subagent or /cf-docs durable objects BEFORE the class was written, and the confirmed import path is recorded in the SUMMARY
    - apps/atlas/src/coordinator.ts declares class AtlasCoordinator extends DurableObject<Env> (grep -c 'class AtlasCoordinator' apps/atlas/src/coordinator.ts >= 1)
    - The staleness threshold is 5 minutes (D-10): the source contains 5 * 60_000 (or equivalent 300_000 / 5*60*1000); grep -Eq '5 ?[*] ?60' succeeds
    - alarm() reschedules itself (a setAlarm(Date.now() + 60_000) call exists at the END of alarm(), so the heartbeat never stops)
    - When stale, the DO emits a P1 incident onto the Wire (an env.WIRE.send(...) with severity P1, or flag(env,"P1",...), exists on the stale branch) - Atlas escalates to Flagger via the Wire, never by writing the Vault directly
    - Owner-local date derived via Intl with timeZone:'America/Toronto' (never naive new Date()); grep -q 'America/Toronto' succeeds
    - No Vault/Gmail/Codex write and NO consumers / async queue( handler appears in this file (Pillar 1 - producer only): grep -cE 'consumers|async queue' apps/atlas/src/coordinator.ts == 0
    - pnpm --filter atlas typecheck exits 0
  </acceptance_criteria>
  <done>The AtlasCoordinator DO compiles against the confirmed agents@0.14.1 API, addressed as getByName("root"), self-monitoring on a 1-min alarm with a 5-min staleness -> P1-to-Flagger self-flag (D-10), producer-only.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the no-op agent service-binding RPC target (D-11) + the scheduled() dispatcher in index.ts (replaces the Wave-1 hello-world)</name>
  <files>apps/atlas/src/noop-agent.ts, apps/atlas/src/index.ts</files>
  <read_first>
    - apps/atlas/src/index.ts (CURRENT state - the Wave-1 minimal hello-world fetch handler from 00-01; this plan REPLACES it with the scheduled() dispatcher. NOTE the outline handoff: 00-06 later re-owns this file to compose the OAuthProvider default export WITH this dispatcher - so keep the dispatcher cleanly separable and do NOT add the OAuthProvider here)
    - .planning/phases/00-spine/00-PATTERNS.md "apps/atlas/src/index.ts (controller)" section (lines 77-100 - the scheduled() dispatcher pattern + the satisfies ExportedHandler<Env> rule + the localDate(env) Intl/America/Toronto rule + the idempotency-key rule) and "Shared Patterns" (the 6.4 contract, the canonical binding names, structured idempotency keys)
    - .planning/phases/00-spine/00-RESEARCH.md "Cron dispatch -> no-op invoke -> Wire send (SPINE-01)" code block (lines 377-395 - copy the dispatcher shape) + the Architecture diagram (lines 152-202, the cron -> service-binding RPC -> WIRE.send path) + D-11 (invokeAgent transport = service-binding RPC, Worker-to-Worker, no public HTTP)
    - docs/agents/atlas.md "What it does" (schedules via Cron Triggers; routes onto the Wire; ensures events reach Steward in order; does NOT fetch for Steward, does NOT mutate any external system)
    - CLAUDE.md "Canonical conventions" (the Wire event field set, structured idempotency-key formats e.g. atlas:noop:<date>, WIRE producer binding) + "Gotchas" (cron Triggers are UTC-only; 07:45 ET = 45 12 * * * EST / 45 11 * * * EDT; satisfies not annotation)
    - packages/wire/src/index.ts + packages/wire/src/send.ts (from 00-02 - the WireEvent type + the producer send helper to import; do NOT re-declare the schema)
  </read_first>
  <action>
    Write apps/atlas/src/noop-agent.ts as the D-11 service-binding RPC target: a no-domain-work entrypoint exposing a single tick(params) RPC method that returns a small acknowledgement (e.g. { ok: true, at: Date.now() }). Use the agents@0.14.1 / Cloudflare WorkerEntrypoint-style RPC surface confirmed in Task 1 (re-confirm via /cf-docs if the service-binding RPC entrypoint shape differs from the DO base class). This is the "no-op agent" SPINE-01 schedules - it performs NO domain work; it exists only to prove the schedule -> invoke leg over a service binding (no public HTTP surface, per D-11).

    REPLACE apps/atlas/src/index.ts (currently the Wave-1 hello-world) with the dispatcher per RESEARCH lines 377-395 / PATTERNS lines 81-96. Default export object whose scheduled(controller, env, ctx) does switch (controller.cron) { case "45 12 * * *": { ... } } (the EST form of the 07:45 ET Filer-sweep slot; the EDT form 45 11 * * * is documented in docs/03-scheduling.md by 00-01 - Phase 0 only needs the dispatcher branch to EXIST; the cron LINES first fire in Phase 1, so do NOT add triggers.crons to wrangler.jsonc here). Inside the branch (SPINE-01, in order): (1) invoke the no-op agent over the service-binding RPC (D-11) - await env.NOOP.tick({ note: "phase-0 smoke" }) against the services binding; (2) THEN route onto the Wire via the 00-02 producer helper or env.WIRE.send with agent:"Atlas", type:"noop.tick", entity:"spine", op:"append", payload:{ note:"phase-0 smoke" }, idempotencyKey: a template literal atlas:noop:${localDate(env)}. Build localDate(env) (or import a shared helper) using Intl with timeZone:'America/Toronto' - STABLE + structured + date-derived idempotency key (atlas:noop:<YYYY-MM-DD>); NEVER crypto.randomUUID() for scheduled work (RESEARCH anti-patterns; CLAUDE.md gotcha). The default export uses satisfies ExportedHandler<Env> (NOT the : ExportedHandler<Env> annotation). RE-EXPORT the AtlasCoordinator class from this file (export { AtlasCoordinator } from "./coordinator") so the new_sqlite_classes:["AtlasCoordinator"] migration in the 00-01 wrangler.jsonc resolves against the main entry. Declare/extend the no-op services binding in the Env type usage so env.NOOP typechecks. The actual services binding line belongs in apps/atlas/wrangler.jsonc (owned by 00-01) - if 00-01 wrangler.jsonc lacks a services self-binding for the no-op target, note it in the SUMMARY as a wiring follow-up; do NOT edit wrangler.jsonc in this plan (it is 00-01 file). Atlas declares producer WIRE only and NO queues.consumers block anywhere (Pillar 1).
  </action>
  <verify>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && pnpm --filter atlas typecheck && grep -q 'scheduled' apps/atlas/src/index.ts && grep -Eq 'WIRE\.send|send\(' apps/atlas/src/index.ts && grep -q 'satisfies ExportedHandler<Env>' apps/atlas/src/index.ts && grep -q 'atlas:noop:' apps/atlas/src/index.ts && grep -q 'export { AtlasCoordinator }' apps/atlas/src/index.ts && [ "$(grep -cE 'consumers|async queue' apps/atlas/src/index.ts)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - apps/atlas/src/noop-agent.ts exports a no-op RPC target with a single tick() method that does NO domain work (grep -q 'tick' apps/atlas/src/noop-agent.ts; grep -iq 'noop' apps/atlas/src/noop-agent.ts)
    - apps/atlas/src/index.ts default export has a scheduled() handler with a switch (controller.cron) branch on "45 12 * * *" (grep -q 'scheduled' and grep -q '45 12' apps/atlas/src/index.ts)
    - Inside the branch the no-op agent is invoked over the service binding BEFORE the Wire send (an env.NOOP.tick / service-binding RPC call precedes env.WIRE.send in source order)
    - The event routed onto the Wire is the canonical 6.4 shape (agent/type/entity/op/payload/idempotencyKey) with op one of increment|upsert|append (here append) and a structured idempotencyKey atlas:noop:<date> (grep -q 'atlas:noop:' apps/atlas/src/index.ts); no crypto.randomUUID() in the file (grep -c 'randomUUID' apps/atlas/src/index.ts == 0)
    - Default export uses satisfies ExportedHandler<Env>, not the : ExportedHandler<Env> annotation (grep -q 'satisfies ExportedHandler<Env>')
    - AtlasCoordinator is re-exported (grep -q 'export { AtlasCoordinator }' apps/atlas/src/index.ts) so the DO migration resolves; the OAuthProvider default export is NOT present (that lands in 00-06): grep -c 'OAuthProvider' apps/atlas/src/index.ts == 0
    - NO queues.consumers block / async queue( handler in index.ts (Pillar 1 - producer only): grep -cE 'consumers|async queue' apps/atlas/src/index.ts == 0
    - pnpm --filter atlas typecheck exits 0
  </acceptance_criteria>
  <done>The scheduled() dispatcher invokes the no-op agent via service-binding RPC (D-11) then routes a canonical 6.4 event onto the Wire with a structured atlas:noop:<date> idempotency key, re-exports the DO, and stays producer-only - replacing the Wave-1 hello-world without introducing the OAuthProvider.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SPINE-01 + heartbeat tests (Wire-contract, single-instance, failure-path) in workerd</name>
  <files>apps/atlas/test/scheduled.test.ts, apps/atlas/test/heartbeat.test.ts</files>
  <behavior>
    - scheduled.test.ts (SPINE-01 / Wire-contract): driving the scheduled() handler for cron "45 12 * * *" results in exactly one canonical 6.4 event on the Wire whose shape validates against the 00-02 WireEvent zod schema, with agent "Atlas", op "append", and idempotencyKey matching ^atlas:noop:\d{4}-\d{2}-\d{2}$; the no-op agent tick() is invoked before the send.
    - heartbeat.test.ts (healthy): after beat(), invoking alarm() within 5 minutes emits NO P1 incident (no heartbeat.stale event on the Wire).
    - heartbeat.test.ts (stale -> P1, failure-path): with lastBeat older than 5 minutes, alarm() emits exactly one heartbeat.stale event with payload.severity "P1" toward Flagger, and reschedules the next alarm (getAlarm() is non-null afterwards).
    - heartbeat.test.ts (single instance / Pillar 1): two env.ATLAS.getByName("root") handles address the SAME DO instance (a value written via one is visible via the other).
  </behavior>
  <read_first>
    - .planning/phases/00-spine/00-PATTERNS.md "Test files" section (lines 270-286 - the cloudflare:test harness usage: import { env } from "cloudflare:test", isolatedStorage resets per test, getByName addressing) + the CI-invariant / Definition-of-Done note (lines 328-332: Wire-contract test + failure-path->Flagger-severity test)
    - .planning/phases/00-spine/00-RESEARCH.md "Validation Architecture" / Test Map references + the replay-test example structure (the cloudflare:test import + getByName pattern to mirror; this plan tests Atlas, not Steward, so adapt to scheduled()/alarm())
    - apps/atlas/vitest.config.ts (from 00-01 - the defineWorkersConfig pool with wrangler.configPath, compatibilityFlags:["nodejs_compat"], isolatedStorage:true) and apps/atlas/src/index.ts + apps/atlas/src/coordinator.ts (the units under test from Tasks 1-2) + packages/wire/src/contract.ts (the WireEvent zod schema to validate emitted events against)
    - CLAUDE.md "Definition of Done" (a Wire-contract test for emitted events + a failure-path test asserting the right Flagger severity) and "Gotchas" (workerd/vitest force TZ=UTC - the date in the idempotency key is UTC-derived in tests; assert the regex shape, not a specific calendar date)
  </read_first>
  <action>
    Write both tests using the @cloudflare/vitest-pool-workers harness (import { env } from "cloudflare:test"; the pool runs real workerd with isolatedStorage:true resetting DO storage per test). To capture Wire emissions, intercept the WIRE producer: either bind a test queue and read it, or stub/spy env.WIRE.send to collect events (mirror the harness approach the 00-02 wire tests establish; confirm the available capture mechanism from packages/wire/test/contract.test.ts).

    scheduled.test.ts: construct a ScheduledController-shaped object with cron "45 12 * * *" and invoke the worker default export scheduled(controller, env, ctx). Assert: (a) exactly one event was sent to the Wire; (b) it parses cleanly with WireEvent.parse(...) from the 00-02 wire package (Wire-contract test); (c) event.agent === "Atlas", event.op === "append", and event.idempotencyKey matches ^atlas:noop:\d{4}-\d{2}-\d{2}$ (structured key, not a UUID); (d) the no-op agent tick() ran before the send (spy on the NOOP service binding and assert call order).

    heartbeat.test.ts: get the DO via env.ATLAS.getByName("root"). Healthy case: call beat(), then alarm() (or simulate the alarm path) and assert NO heartbeat.stale/P1 event reached the Wire. Stale case: force lastBeat into the past (>5 min - via a DO method or by manipulating storage), invoke alarm(), and assert exactly one heartbeat.stale event with payload.severity === "P1" was sent (failure-path -> correct Flagger severity, D-10), and that the alarm was rescheduled. Single-instance case: obtain two getByName("root") handles, write a marker via one (e.g. beat()), and assert it is visible via the other (proves one name = one DO = one lock; Pillar 1 / Pitfall 4).
  </action>
  <verify>
    <automated>cd /Users/danielchahine/Desktop/Programs/Atlas && pnpm --filter atlas test</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter atlas test runs both suites in workerd (the vitest-pool-workers pool) and all cases pass
    - scheduled.test.ts validates the emitted event with the 00-02 WireEvent zod schema (a WireEvent.parse / safeParse call is present) - the Wire-contract test required by the Definition of Done
    - scheduled.test.ts asserts the idempotencyKey matches the structured atlas:noop:<YYYY-MM-DD> pattern (a regex assertion on ^atlas:noop:\\d{4}-\\d{2}-\\d{2}$ is present) and asserts the no-op tick() runs before WIRE.send
    - heartbeat.test.ts contains a >5-min-stale case asserting exactly one P1 heartbeat.stale event (the failure-path -> Flagger-severity test, D-10) AND a healthy case asserting no P1 emit
    - heartbeat.test.ts contains a single-instance assertion: two getByName("root") handles resolve to the same DO (grep -q 'getByName' apps/atlas/test/heartbeat.test.ts)
  </acceptance_criteria>
  <done>SPINE-01 is proven by an automated workerd test (cron -> no-op invoke -> canonical 6.4 Wire event with a structured key), and the heartbeat self-flag (D-10) plus the single-instance invariant (Pillar 1) are covered by passing failure-path and single-instance tests.</done>
</task>

</tasks>

<threat_model>
ASVS L1; security_enforcement ON; the orchestrator runtime is in scope. Block on any `high`-disposition threat.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron Trigger -> Atlas scheduled() | The platform fires the scheduled() handler on a cron string; the dispatcher must route only known crons (switch on controller.cron) and never trust an arbitrary cron to do work |
| Atlas Worker -> no-op agent (service-binding RPC, D-11) | Worker-to-Worker RPC over a private service binding (no public HTTP surface) - the only invocation path; keeps the no-op target off the public internet |
| Atlas (producer) -> the Wire (atlas-wire Queue) | Atlas enqueues events; it must remain a producer ONLY and never a consumer (Pillar 1 - Steward is the sole consumer) |
| AtlasCoordinator DO -> Flagger (P1 incident on the Wire) | The self-supervision escalation path; Atlas escalates by emitting onto the Wire, never by writing the Vault/Gmail/Codex directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-31 | Spoofing | no-op agent invocation (D-11) | mitigate | Invoke only over the private service binding (Worker-to-Worker RPC, no public HTTP route) - acceptance asserts no fetch/HTTP route is exposed for the no-op target; the binding, not a URL, is the contract |
| T-00-32 | Tampering | scheduled() dispatcher routing | mitigate | switch(controller.cron) routes ONLY the known case; an unknown cron is a no-op (default branch does nothing) - the dispatcher never executes work for an unrecognized trigger |
| T-00-33 | Elevation of Privilege | Atlas posture (Pillar 1) | mitigate | Atlas is producer-only: no queues.consumers block and no async queue() handler in coordinator.ts or index.ts (grep gate == 0 in both); Atlas never writes the Vault/Gmail/Codex (no such call in any owned file) |
| T-00-34 | Repudiation | heartbeat staleness (D-10) | mitigate | The 1-min alarm() self-monitors; >5-min staleness emits a P1 heartbeat.stale incident to Flagger via the Wire - a stuck orchestrator surfaces an incident instead of silently halting; covered by the stale-case failure-path test |
| T-00-35 | Tampering | replayed / re-fired cron | mitigate | The emitted event uses a STABLE structured idempotencyKey (atlas:noop:<date>), never crypto.randomUUID() - a re-fired or missed-then-recovered cron replays as a downstream no-op via Steward ledger dedup (00-04); acceptance greps that no randomUUID is used |
| T-00-36 | Information Disclosure | DO storage + event payloads | accept | The DO stores only a lastBeat timestamp and the smoke event payload is a static note - no secrets, no PII; secrets bindings (Secrets Store) are not touched by this plan (they land in 00-06) |
| T-00-37 | Denial of Service | alarm() rescheduling | mitigate | alarm() ALWAYS reschedules the next alarm at the END of the handler (acceptance asserts the trailing setAlarm) so the heartbeat cannot silently stop; one alarm per DO (setting a new time overwrites - no alarm storm) |
| T-00-SC | Tampering | npm/pnpm installs | accept | No NEW package-manager install in this plan - it consumes the agents@0.14.1 / zod deps already installed and audited [OK] in 00-01 (RESEARCH Package Legitimacy Audit, npm ecosystem); no [ASSUMED]/[SLOP] packages added |
</threat_model>

<verification>
- pnpm --filter atlas typecheck exits 0 (the DO, the no-op target, and the dispatcher compile against the confirmed agents@0.14.1 API and the 00-02 Env/WireEvent contracts)
- pnpm --filter atlas test passes both suites in workerd: SPINE-01 (cron -> no-op invoke -> canonical 6.4 Wire event, validated by the WireEvent zod schema, with a structured atlas:noop:<date> key) and the heartbeat (healthy no-emit, >5-min-stale P1 emit, single-instance getByName("root"))
- grep -cE 'consumers|async queue' apps/atlas/src/index.ts == 0 AND grep -cE 'consumers|async queue' apps/atlas/src/coordinator.ts == 0 (Pillar 1 - Atlas is never an atlas-wire consumer)
- grep -c 'randomUUID' apps/atlas/src/index.ts == 0 (structured idempotency keys only for scheduled work)
- grep -c 'OAuthProvider' apps/atlas/src/index.ts == 0 (the OAuth front door is deliberately deferred to 00-06; this plan ships only the dispatcher)
- grep -q 'export { AtlasCoordinator }' apps/atlas/src/index.ts (the DO is re-exported so the 00-01 new_sqlite_classes migration resolves)
</verification>

<success_criteria>
- SPINE-01 satisfied: Atlas schedules a no-op agent (service-binding RPC, D-11) and routes a canonical 6.4 message onto the Wire, proven by an automated workerd test (ROADMAP Phase-0 Success Criterion 1)
- AtlasCoordinator DO compiles against the confirmed agents@0.14.1 base-class API (confirmation recorded in the SUMMARY), addressed only as getByName("root")
- Heartbeat self-supervision works (D-10): 1-min alarm cadence, >5-min staleness emits a P1 heartbeat.stale incident to Flagger via the Wire, alarm always reschedules
- The emitted event carries a stable structured idempotencyKey (atlas:noop:<date>) - no crypto.randomUUID() for scheduled work
- Pillar 1 preserved: Atlas is producer-only (no consumers block / queue handler anywhere it owns), never writes the Vault/Gmail/Codex
- The Wave-1 hello-world index.ts is replaced by the dispatcher WITHOUT introducing the OAuthProvider default export (that handoff is reserved for 00-06)
</success_criteria>

<output>
Create `.planning/phases/00-spine/00-03-SUMMARY.md` when done. Include an "API confirmation" note recording the confirmed agents@0.14.1 DurableObject<Env> import path + ctx.storage/alarm() signatures, and a "wiring follow-up" note if the no-op services self-binding is not yet present in the 00-01 apps/atlas/wrangler.jsonc.
</output>
