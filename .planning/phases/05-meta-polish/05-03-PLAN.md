---
phase: 05-meta-polish
plan: 03
type: tdd
wave: 3
depends_on: [05-02]
files_modified:
  - apps/librarian/test/apply-migrations.ts
  - apps/librarian/test/wire-contract.test.ts
  - apps/librarian/test/replay.test.ts
  - apps/librarian/test/failure.test.ts
autonomous: true
requirements: [META-01]
must_haves:
  truths:
    - "The emitted Wire event is §6.4-valid with op:upsert and a structured librarian:<slug>:save idempotencyKey"
    - "Replaying the same Librarian upsert through Steward is a no-op (meta.changes === 0): no second note, no second vault_outbox row, uses unchanged"
    - "A wrong/missing Bearer token (and a missing binding) returns 401, fail-closed"
    - "Empty/oversized prompt → P4/P3 Flagger flag and NO Wire event; borderline dedupe → P4 flag + keep-separate"
  artifacts:
    - path: "apps/librarian/test/wire-contract.test.ts"
      provides: "Definition-of-Done Test 1 — Wire-contract shape + structured key"
      contains: "WireEvent"
    - path: "apps/librarian/test/replay.test.ts"
      provides: "Definition-of-Done Test 2 — replay-through-Steward meta.changes===0 + bump-not-clone"
      contains: "applied"
    - path: "apps/librarian/test/failure.test.ts"
      provides: "Definition-of-Done Test 3 — Bearer 401 + empty/oversized + borderline Flagger-severity"
      contains: "401"
    - path: "apps/librarian/test/apply-migrations.ts"
      provides: "D1 migration runner for workerd tests (includes 0008)"
      contains: "applyD1Migrations"
  key_links:
    - from: "apps/librarian/test/replay.test.ts"
      to: "StewardWriter.apply (apps/steward)"
      via: "STEWARD_LOCK.getByName('vault').apply(evt) twice"
      pattern: "applied"
    - from: "apps/librarian/test/wire-contract.test.ts"
      to: "@atlas/wire WireEvent"
      via: "WireEvent.parse(emitted)"
      pattern: "WireEvent.parse"
---

<objective>
Ship the three mandatory Atlas Definition-of-Done tests for Librarian (CLAUDE.md): a Wire-contract test, a replay-through-Steward idempotency test (`meta.changes === 0`), and a failure-path test asserting the right Flagger severities + the Bearer 401. Add the Wave-0 test scaffolding (vitest workerd config support files) these depend on.

Purpose: These tests are the executable proof that META-01 satisfies the 05-VALIDATION per-requirement test map (Dimension 8) and the project's non-negotiable three-test PR rule. They lock in the idempotency invariant (replay leaves `uses` unchanged, no clone), the producer-only Wire contract, and the fail-closed Bearer gate.
Output: Four test files under `apps/librarian/test/`. After this plan, `pnpm --filter librarian test` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-meta-polish/05-RESEARCH.md
@.planning/phases/05-meta-polish/05-PATTERNS.md
@.planning/phases/05-meta-polish/05-VALIDATION.md
@.planning/phases/05-meta-polish/05-02-SUMMARY.md

<interfaces>
<!-- Test contracts (verified from source). The Librarian Worker + 05-01 op-mapping must be merged first. -->

From @atlas/wire:  export const WireEvent: zod schema (the only definition — packages/wire/src/contract.ts)
From apps/steward/src/steward.ts:  class StewardWriter — `apply(e: WireEvent): Promise<{ applied: boolean }>` (wraps applyEvent in blockConcurrencyWhile); bound as STEWARD_LOCK DO, getByName("vault")
From packages/shared:  RawIncident type; flag() enqueues onto INCIDENTS

Librarian fetch handler (05-02): default export `fetch(request, env)`; POST /prompt/save; 401 on bad auth; emits 0 or 1 WireEvent via env.WIRE.send; flags via env.INCIDENTS.send.

vitest config (05-02 created apps/librarian/vitest.config.ts): setupFiles ["./test/apply-migrations.ts"], provide { migrations }, cloudflareTest with wrangler.test.jsonc + nodejs_compat.
</interfaces>
</context>

<feature>
  <name>Librarian Definition-of-Done test suite</name>
  <files>apps/librarian/test/apply-migrations.ts, apps/librarian/test/wire-contract.test.ts, apps/librarian/test/replay.test.ts, apps/librarian/test/failure.test.ts</files>
  <behavior>
    Wire-contract (Test 1): a valid POST /prompt/save emits exactly one WireEvent; WireEvent.parse(emitted) does not throw; agent==="librarian", type==="prompt.save", entity==="prompt", op==="upsert"; idempotencyKey matches /^librarian:[a-z0-9-]+:save$/ on first save; payload.fullNote===true and String(payload.notePath) matches /^Prompts\//.
    Replay (Test 2): applying the same Librarian upsert WireEvent twice through StewardWriter.apply returns { applied: true } then { applied: false }; the prompts/vault_outbox state has ONE pending row (not two); a dedupe-bump event on the SAME slug does not create a second note (upsert, not clone) — uses is bumped in D1, not double-counted on replay.
    Failure (Test 3): missing Authorization header → 401; missing ATLAS_LIBRARIAN_TOKEN binding → 401 (fail-closed); wrong token → 401; empty prompt body → one P4 incident (kind "empty_capture") and zero WireEvents; oversized (>50KB) prompt → one P3 incident and zero WireEvents; borderline dedupe → one P4 incident (kind "dedupe_borderline") AND a new-slug WireEvent (keep-separate, never silent merge).
  </behavior>
  <implementation>
    Copy `apps/librarian/test/apply-migrations.ts` verbatim from `packages/gate/test/apply-migrations.ts` (identical to `apps/flagger/test/apply-migrations.ts`) — beforeAll applies all migrations (now including 0008) to env.DB. Write `wire-contract.test.ts` and `failure.test.ts` against the Librarian fetch handler using a spy env (vi.fn stubs for WIRE.send / INCIDENTS.send / ATLAS_LIBRARIAN_TOKEN.get / CONFIG.get / DB) — mirror the spy-env + makeAuthEnv helpers from `apps/flagger/test/routing.test.ts` (lines 47-98) and `apps/flagger/test/ack-auth.test.ts` (lines 29-55). Write `replay.test.ts` against the real StewardWriter DO in workerd — copy the structure of `apps/steward/test/replay.test.ts` (lines 19-83): get STEWARD_LOCK.getByName("vault"), build a librarian upsert event with payload.fullNote/notePath/noteBody, apply twice, assert {applied:true} then {applied:false}. This test depends on the 05-01 op-mapping fullNote+PUT branch being merged (else toOutboxIntent throws) — it validates the whole META-01 path end-to-end through Steward.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Wave-0 scaffolding + Wire-contract + failure tests</name>
  <files>apps/librarian/test/apply-migrations.ts, apps/librarian/test/wire-contract.test.ts, apps/librarian/test/failure.test.ts</files>
  <read_first>
    - packages/gate/test/apply-migrations.ts (copy verbatim)
    - apps/flagger/test/ack-auth.test.ts lines 1-60 (the Bearer-gate failure test + makeAuthEnv pattern)
    - apps/flagger/test/routing.test.ts lines 24-98 + 254-292 (spy-env / fakeBatch helpers + malformed→P4 path)
    - apps/librarian/src/index.ts, apps/librarian/src/auth.ts (05-02 — the handler under test)
    - .planning/phases/05-meta-polish/05-PATTERNS.md sections "apps/librarian/test/apply-migrations.ts", "apps/librarian/test/wire-contract.test.ts", "apps/librarian/test/failure.test.ts"
    - .planning/phases/05-meta-polish/05-VALIDATION.md Per-Task Verification Map (the exact behaviors + threat refs)
  </read_first>
  <action>
    Copy `apps/librarian/test/apply-migrations.ts` from the gate analog (beforeAll → applyD1Migrations(env.DB, inject("migrations"))). `wire-contract.test.ts`: drive a valid `POST /prompt/save` (spy WIRE.send captures the event), assert the §6.4 shape via `WireEvent.parse`, the literal field values (agent/type/entity/op), the `^librarian:[a-z0-9-]+:save$` key, and `payload.fullNote===true` + `notePath` under `Prompts/`. `failure.test.ts`: assert 401 for missing header / missing binding (fail-closed) / wrong token (T-5-Auth); empty body → one P4 INCIDENTS event kind "empty_capture" + zero WIRE events; oversized (>50KB) → one P3 INCIDENTS event + zero WIRE events (T-5-DoS); borderline dedupe → one P4 INCIDENTS event kind "dedupe_borderline" + a new-slug WIRE event (keep-separate). Use vi.fn() spies; seed DB with the existing prompts for the borderline case (or stub the dedupe path). Do not assert anything that requires the live Obsidian bridge (manual-only per VALIDATION).
  </action>
  <verify>
    <automated>pnpm --filter librarian test wire-contract failure</automated>
  </verify>
  <acceptance_criteria>
    - `apps/librarian/test/apply-migrations.ts` exists and calls `applyD1Migrations`.
    - `wire-contract.test.ts` asserts `WireEvent.parse(emitted)` does not throw and the idempotencyKey matches `^librarian:[a-z0-9-]+:save$`.
    - `failure.test.ts` asserts 401 for missing header, missing binding, and wrong token.
    - `failure.test.ts` asserts empty → P4 + 0 Wire events; oversized → P3 + 0 Wire events; borderline → P4 + a new-slug Wire event.
    - `pnpm --filter librarian test wire-contract failure` exits 0.
  </acceptance_criteria>
  <done>Definition-of-Done Tests 1 + 3 (Wire-contract + failure-path → Flagger severity + Bearer 401) pass.</done>
</task>

<task type="tdd">
  <name>Task 2: Replay-through-Steward idempotency test</name>
  <files>apps/librarian/test/replay.test.ts</files>
  <read_first>
    - apps/steward/test/replay.test.ts lines 1-83 (the exact StewardWriter.apply replay structure to mirror)
    - apps/steward/src/steward.ts (the StewardWriter DO + apply() method + STEWARD_LOCK binding)
    - packages/steward-core/src/op-mapping.ts (the 05-01 fullNote PUT branch this test exercises end-to-end)
    - apps/librarian/test/apply-migrations.ts (Task 1 — migrations incl. 0008 must be applied)
    - .planning/phases/05-meta-polish/05-PATTERNS.md "apps/librarian/test/replay.test.ts"
  </read_first>
  <action>
    `replay.test.ts`: in workerd, get `STEWARD_LOCK.getByName("vault")`, construct a Librarian upsert WireEvent `{ agent:"librarian", type:"prompt.save", entity:"prompt", op:"upsert", payload:{ fullNote:true, notePath:"Prompts/test-prompt.md", noteBody:"---\ntitle: Test Prompt\n---\n\nContent." }, idempotencyKey:"librarian:test-prompt:save" }`, apply it twice. Assert first `{ applied: true }`, second `{ applied: false }` (replay no-op, meta.changes===0). Then assert exactly ONE pending `vault_outbox` row exists for that idem (query env.DB) — replay did not enqueue a second write. Add a second case: a dedupe-bump event with key `librarian:test-prompt:save:<date>` is a distinct ledger entry (applies true once) but writes to the SAME notePath (upsert on the same slug — no clone). This test confirms the whole META-01 path renders through the 05-01 op-mapping fullNote PUT branch (if that branch is missing, toOutboxIntent throws — a useful integration signal).
  </action>
  <verify>
    <automated>pnpm --filter librarian test replay</automated>
  </verify>
  <acceptance_criteria>
    - `apps/librarian/test/replay.test.ts` applies the same Librarian upsert event twice through `StewardWriter.apply` and asserts `{ applied: true }` then `{ applied: false }`.
    - The test asserts exactly one pending `vault_outbox` row for the idem after the replay (no second write enqueued).
    - The test exercises the 05-01 fullNote PUT branch (notePath under `Prompts/`) end-to-end.
    - `pnpm --filter librarian test replay` exits 0.
  </acceptance_criteria>
  <done>Definition-of-Done Test 2 passes: replay is a no-op (meta.changes===0), no clone, uses unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → Librarian fetch handler | Tests assert the auth + validation boundary is fail-closed |
| Test harness → StewardWriter DO | Tests assert the idempotency ledger rejects replays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-Auth | Spoofing | failure.test.ts Bearer assertions | mitigate | Tests prove 401 on missing header, missing binding (fail-closed), and wrong token — locks the constant-time gate against regression. |
| T-5-DoS | Denial of Service | failure.test.ts oversized assertion | mitigate | Test proves a >50KB prompt is rejected (P3 flag, no Wire emit), keeping under the 128KB Queue cap. |
| T-5-Replay | Tampering | replay.test.ts | mitigate | Test proves a replayed upsert is `{ applied: false }` with one vault_outbox row — `uses` cannot be double-counted, no clone. |
| T-5-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages (RESEARCH Package Legitimacy Audit: none). Tests use existing workspace devDeps only. No install task. |
</threat_model>

<verification>
- `pnpm --filter librarian test` exits 0 (all three Definition-of-Done tests green).
- Full suite green: `pnpm test` (no regression in the Phase 0-4 baseline).
- `nyquist_compliant` satisfied: every META-01 behavior in 05-VALIDATION has a passing automated test.
</verification>

<success_criteria>
- The three mandatory Atlas tests for Librarian (Wire-contract, replay-through-Steward `meta.changes===0`, failure-path → Flagger severity) all pass.
- The Bearer gate is proven fail-closed; the idempotency invariant (no clone, no double-count) is proven through the real StewardWriter DO.
</success_criteria>

<output>
Create `.planning/phases/05-meta-polish/05-03-SUMMARY.md` when done.
</output>
