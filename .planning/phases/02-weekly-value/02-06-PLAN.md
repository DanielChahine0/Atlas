---
phase: 02-weekly-value
plan: 06
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/herald/src/index.ts
  - apps/herald/src/weekly.ts
  - apps/herald/test/weekly.test.ts
  - apps/herald/test/heartbeat.test.ts
autonomous: true
requirements: [WEEKLY-01]
must_haves:
  truths:
    - "Herald gains a weekly() mode that drafts a Friday week-in-review email (7-day window) — draft-only, no send (D2-10)"
    - "The weekly draft passes the SAME redaction guardrail as the daily digest — zero 2FA codes / reset links / login URLs"
    - "Herald.weekly emits a digest Wire event (herald:weekly:<date>) feeding the 16:30 weekly-review Vault build"
    - "Herald emits a kind:heartbeat incident on a successful run"
  artifacts:
    - path: "apps/herald/src/weekly.ts"
      provides: "week-in-review builder over the 7-day window"
      contains: "weekly"
    - path: "apps/herald/src/index.ts"
      provides: "Herald.weekly() WorkerEntrypoint method alongside daily()"
      contains: "async weekly"
  key_links:
    - from: "apps/herald/src/weekly.ts"
      to: "guardDigestOutput"
      via: "redaction guardrail (leak → block draft + P2)"
      pattern: "guard"
    - from: "apps/herald/src/index.ts"
      to: "atlas-wire"
      via: "send() herald:weekly:<date> digest event"
      pattern: "herald:weekly"
---

<objective>
Add Herald's weekly mode (D2-10, deferred from Phase 1 by D1-02): a Friday "week-in-review" retrospective over the 7-day window (action-required still open, waiting-on, VIP threads, items that slipped) — draft-only (`gmail.compose`, no send), reusing the SAME redaction guardrail as the daily digest, plus a digest Wire event (`herald:weekly:<date>`) feeding the 16:30 weekly-review Vault build. Also add Herald's `kind:"heartbeat"` incident emit (kept here so `apps/herald` has a single owner this phase).

Purpose: WEEKLY-01 success criterion 1 — a weekly email review must land. Reuses Herald's draft + redaction path.
Output: `Herald.weekly()` + week-in-review builder + weekly + heartbeat tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-weekly-value/02-CONTEXT.md
@.planning/phases/02-weekly-value/02-RESEARCH.md
@.planning/phases/02-weekly-value/02-PATTERNS.md
@.planning/phases/02-weekly-value/02-VALIDATION.md
@.planning/phases/02-weekly-value/02-01-SUMMARY.md

<interfaces>
From @atlas/shared (after 02-01): `localDate(env?)`, `flag(env, sev, title, detail?, { sourceAgent, kind })` (→ INCIDENTS), `RawIncident`.
From @atlas/wire: `send(env, event)`, `WireEvent`.
Existing Herald (apps/herald/src/index.ts): `class Herald extends WorkerEntrypoint<Env>` with `async daily(params?: { date?; threads?; tools? }): Promise<DailyResult>` (line ~115). The daily idempotency key is `herald:daily:<date>` (line ~75).
Existing redaction: `guardDigestOutput()` in apps/herald/src/guardrail.ts (line ~33/~38) — a leak BLOCKS the draft + raises P2. The weekly mode is NOT exempt (non-negotiable security invariant).
Existing draft tool: `HeraldGmailTools.createDraft(...)` — no send method exists (gmail.compose, draft-only). Reuse it.

Weekly idempotency key: `herald:weekly:<date>`. Weekly emits TWO outputs: (1) a `digest` Wire event op:"upsert" entity:"email" feeding the 16:30 build; (2) a Gmail DRAFT (7-day review), no send.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Herald week-in-review builder (draft-only, redaction-guarded)</name>
  <files>apps/herald/src/weekly.ts, apps/herald/test/weekly.test.ts</files>
  <read_first>
    - apps/herald/src/index.ts (the existing daily() method + DailyResult shape + how daily builds sections and calls createDraft — weekly mirrors this for a 7-day window)
    - apps/herald/src/guardrail.ts (guardDigestOutput — the redaction guardrail that BLOCKS a draft on a 2FA/reset-link/login-URL leak + raises P2; weekly MUST reuse it)
    - .planning/phases/02-weekly-value/02-PATTERNS.md → `apps/herald/src/index.ts` section (the weekly() method pattern, idempotency key, redaction reuse, the TWO-event landmine, lines 710-738)
    - .planning/phases/02-weekly-value/02-CONTEXT.md D2-10 (week-in-review retrospective: action-required open, waiting-on, VIP threads, slipped items; draft-only; digest event for the 16:30 build)
    - CLAUDE.md security invariant (NEVER surface 2FA codes / reset links / login URLs; prompt instruction alone is NOT sufficient — the guardrail + CI test backstop it)
  </read_first>
  <behavior>
    - Test (weekly): runWeekly(env, date, threads, tools) builds a week-in-review draft over a 7-day window and calls tools.createDraft once (no send method exists). A thread whose body contains a 2FA code / reset link / login URL is caught by guardDigestOutput → the draft is BLOCKED and a P2 flag is raised (kind security_leak_blocked) — the secret never reaches the draft body.
    - Test (weekly): the digest Wire event uses idempotencyKey `herald:weekly:<date>`, op "upsert", entity "email".
  </behavior>
  <action>Create `apps/herald/src/weekly.ts` exporting `runWeekly(env, date, threads, tools)` that builds a week-in-review over the trailing 7 days: sections for action-required still open, waiting-on, VIP threads, and items that slipped. Run EVERY synthesized output through `guardDigestOutput()` (same import as daily) BEFORE drafting — a leak blocks the draft and raises a P2 (`kind:"security_leak_blocked"`). Create the draft via the existing `HeraldGmailTools.createDraft(...)` (draft-only; do NOT add a send method). Return a `WeeklyResult` and the digest payload. Use `localDate(env)` for the date. Create `apps/herald/test/weekly.test.ts` covering the draft-only path AND the redaction-block path (a planted secret must NOT reach the draft).</action>
  <verify>
    <automated>pnpm --filter herald test -- weekly</automated>
  </verify>
  <acceptance_criteria>
    - `apps/herald/src/weekly.ts` builds a 7-day review and calls `createDraft` (no `send` method introduced: `grep -c "\.send(" apps/herald/src/weekly.ts` for a Gmail send == 0)
    - The weekly path runs `guardDigestOutput` BEFORE drafting; a planted 2FA code / reset link / login URL blocks the draft + raises a P2 (weekly redaction test asserts the secret never reaches the draft)
    - `pnpm --filter herald test -- weekly` exits 0
  </acceptance_criteria>
  <done>Week-in-review builder is draft-only and redaction-guarded; weekly test (draft + redaction block) green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Herald.weekly() entrypoint method + digest event + heartbeat emit</name>
  <files>apps/herald/src/index.ts, apps/herald/test/heartbeat.test.ts</files>
  <read_first>
    - apps/herald/src/index.ts (the existing daily() method to mirror; the existing Wire emit + idempotency key `herald:daily:<date>` at line ~75; the WorkerEntrypoint class)
    - apps/herald/src/weekly.ts (runWeekly from Task 1 — the method delegates to it)
    - .planning/phases/02-weekly-value/02-PATTERNS.md → `apps/filer/src/index.ts` "Heartbeat emit pattern" (the kind:heartbeat RawIncident emit) + `apps/herald/src/index.ts` weekly() method shape
    - .planning/phases/02-weekly-value/02-RESEARCH.md Shared Patterns (structured idempotency keys — herald:weekly:<date>)
  </read_first>
  <behavior>
    - Test (heartbeat): after a successful daily() OR weekly() run, Herald emits one `env.INCIDENTS?.send({ source_agent:"Herald", kind:"heartbeat", severity_hint:"P4", title contains "Herald" + date, run_id: date })`. A failed run does NOT emit a heartbeat.
  </behavior>
  <action>In `apps/herald/src/index.ts` add `async weekly(params?: { date?; threads?; tools? }): Promise<WeeklyResult>` alongside `daily()`: resolve `date = params?.date ?? localDate(this.env)`, call `runWeekly(this.env, date, params?.threads ?? [], params?.tools)`, `send()` the `digest` Wire event (`op:"upsert"`, `entity:"email"`, idempotencyKey `herald:weekly:<date>`) feeding the 16:30 build, and at the END of a successful run emit the `kind:"heartbeat"` incident (`source_agent:"Herald"`, severity_hint "P4", run_id date). Add the same heartbeat emit at the end of the existing successful `daily()` path so Herald's slot is monitored. Create `apps/herald/test/heartbeat.test.ts`. Herald still declares NO own cron — Atlas invokes `env.HERALD.weekly(date)` (wired in Plan 02-07).</action>
  <verify>
    <automated>pnpm --filter herald test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/herald/src/index.ts` declares `async weekly(...)` returning a WeeklyResult and emitting a digest event with idempotencyKey `herald:weekly:<date>`
    - Herald emits a `kind:"heartbeat"` incident on a successful daily() and weekly() run (severity_hint P4, run_id = localDate)
    - No Gmail send method added anywhere in Herald (draft-only invariant intact)
    - `pnpm --filter herald test` exits 0 (existing daily tests still green + weekly + heartbeat)
  </acceptance_criteria>
  <done>Herald.weekly() drafts + emits the herald:weekly digest event; heartbeat emit on daily+weekly; full herald suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 7-day email window → weekly draft | Email content is untrusted; redaction guardrail is the boundary |
| Herald → Gmail draft | gmail.compose (draft-only, no send) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-leak | Information Disclosure | a 2FA code / reset link / login URL reaching the weekly draft | mitigate | Reuse guardDigestOutput BEFORE drafting — a leak BLOCKS the draft + raises P2 (kind security_leak_blocked); defense-in-depth (server-side redaction in mcp-google + this guardrail + CI test); weekly is NOT exempt |
| T-02-send | Elevation of Privilege | weekly mode gaining a send capability | mitigate | HeraldGmailTools has no send method (gmail.compose only); acceptance asserts no send introduced |
| T-02-SC | Tampering | npm installs | accept | No new package installs (weekly reuses existing Herald deps) |
</threat_model>

<verification>
- `pnpm --filter herald test` exits 0 (daily regression + weekly + redaction-block + heartbeat)
- No Gmail `send` method anywhere in apps/herald (draft-only invariant)
- Definition-of-Done: Wire-contract test (herald:weekly digest shape) + replay (Steward meta.changes===0) + failure-path (security_leak_blocked P2) present
- `pnpm test` full suite green
</verification>

<success_criteria>
Herald.weekly() drafts a redaction-guarded week-in-review (no send) and emits the herald:weekly digest event feeding the 16:30 build; heartbeat emit on success; full herald suite green.
</success_criteria>

<output>
Create `.planning/phases/02-weekly-value/02-06-SUMMARY.md` when done
</output>
