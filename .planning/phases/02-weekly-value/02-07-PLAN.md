---
phase: 02-weekly-value
plan: 07
type: execute
wave: 3
depends_on: [02-02, 02-03, 02-04, 02-05, 02-06]
files_modified:
  - apps/atlas/src/index.ts
  - apps/atlas/wrangler.jsonc
  - apps/atlas/test/scheduled.test.ts
  - apps/steward/src/index.ts
  - apps/steward/src/weekly-review.ts
  - apps/steward/test/weekly-review.test.ts
  - apps/steward/test/replay.test.ts
autonomous: false
requirements: [WEEKLY-01]
user_setup:
  - service: cloudflare-plan
    why: "Workers Free per-Worker cron cap may not fit 5 Atlas crons (RESEARCH A1, LOW confidence)"
    env_vars: []
    dashboard_config:
      - task: "Verify the per-Worker cron cap accommodates the expanded Atlas crons; upgrade to Workers Paid ($5/mo) ONLY if capped"
        location: "wrangler deploy --dry-run / Cloudflare dashboard"
must_haves:
  truths:
    - "Atlas scheduled() switch gains 4 standalone cron CASES (Headhunter daily-light, Headhunter full, Friday Scout+Herald, Friday 16:30 build) â NOT new MorningChain Workflow steps (D2-11)"
    - "The Friday 16:00 case runs Scout.weekly and Herald.weekly via Promise.allSettled (one failure does not discard the other's result)"
    - "Both EDT and EST cron forms appear as dual cases in the switch (DST-safe, like the existing 45 11/45 12 pattern)"
    - "The 16:30 case kicks Steward.weeklyReviewBuild(date) which compiles the weekly-review Vault build"
    - "Atlas's wrangler crons use the EDT form (active June 2026); a cron-cap human-verify gates the cron expansion"
  artifacts:
    - path: "apps/steward/src/weekly-review.ts"
      provides: "weekly-review build compiler"
      contains: "weeklyReview"
    - path: "apps/atlas/src/index.ts"
      provides: "4 new standalone cron cases in scheduled()"
      contains: "SCOUT.weekly"
  key_links:
    - from: "apps/atlas/src/index.ts"
      to: "Scout/Headhunter/Herald/Steward"
      via: "service-binding RPC in scheduled() cases"
      pattern: "(SCOUT|HEADHUNTER|HERALD|STEWARD)\\."
    - from: "apps/atlas/wrangler.jsonc"
      to: "scout/headhunter services"
      via: "services bindings"
      pattern: "\"service\": \"(scout|headhunter)\""
---

<objective>
Wire Phase 2's cadence into Atlas's `scheduled()` dispatcher (D2-11): add 4 standalone cron CASES (Headhunter daily-light, Headhunter full, Friday Scout+Herald via `Promise.allSettled`, Friday 16:30 weekly-review build) â NOT new MorningChain Workflow steps. Add the Scout/Headhunter service bindings, expand Atlas's wrangler crons (EDT forms, dual EDT/EST cases in the switch for DST), and add the `Steward.weeklyReviewBuild()` method the 16:30 case calls. A blocking human-verify confirms the Workers Free per-Worker cron cap accommodates the expanded crons BEFORE deploying them (RESEARCH A1).

Purpose: WEEKLY-01 criterion 1 â Friday events digest + weekly review land, fan into Steward, then the 16:30 build. Integration wave (needs Scout/Headhunter/Herald-weekly to exist).
Output: Atlas cron cases + service bindings + Steward weekly-review build + cron-cap checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-weekly-value/02-CONTEXT.md
@.planning/phases/02-weekly-value/02-RESEARCH.md
@.planning/phases/02-weekly-value/02-PATTERNS.md
@.planning/phases/02-weekly-value/02-VALIDATION.md
@.planning/phases/02-weekly-value/02-04-SUMMARY.md
@.planning/phases/02-weekly-value/02-05-SUMMARY.md
@.planning/phases/02-weekly-value/02-06-SUMMARY.md

<interfaces>
Existing Atlas dispatcher (apps/atlas/src/index.ts): a `switch (controller.cron)` in `scheduled()` (cases `"45 11 * * 1-5"`, `"45 12 * * 1-5"`, `"45 12 * * *"`). Existing services in apps/atlas/wrangler.jsonc: NOOP, FILER, HERALD, FORGE, SUNDIAL, COMPASS. Existing trigger: `"45 11 * * 1-5"` (EDT) only.

New service bindings to add: `{ "binding": "SCOUT", "service": "scout" }`, `{ "binding": "HEADHUNTER", "service": "headhunter" }`. (HERALD + STEWARD bindings: HERALD already exists; add STEWARD `{ "binding": "STEWARD", "service": "steward" }` so the 16:30 case can RPC it.)

RPC methods (from Plans 02-04/05/06): `env.SCOUT.weekly({ date })`, `env.HEADHUNTER.full({ date })`, `env.HEADHUNTER.deadlines({ date })`, `env.HERALD.weekly({ date })`. NEW (this plan): `env.STEWARD.weeklyReviewBuild({ date })`.

Cron forms (DST, active June 2026 = EDT/UTC-4 â see RESEARCH Pattern 6 + PATTERNS apps/atlas/src/index.ts):
- Headhunter daily-light 09:00 ET: EDT `0 13 * * *` / EST `0 14 * * *`
- Headhunter full Mon 09:00 ET: EDT `0 13 * * 1` / EST `0 14 * * 1`
- Friday Scout+Herald 16:00 ET: EDT `0 20 * * 5` / EST `0 21 * * 5`
- Friday 16:30 build: EDT `30 20 * * 5` / EST `30 21 * * 5`
Wrangler crons use the EDT forms now; the switch carries BOTH forms as dual cases.

Steward currently has only a queue consumer (apps/steward/src/index.ts has a fetch() stub, no WorkerEntrypoint). weeklyReviewBuild must be added as a WorkerEntrypoint method so Atlas can RPC it.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Workers Free per-Worker cron cap before expanding Atlas crons</name>
  <what-built>Plans 02-04/05/06 created Scout, Headhunter, and Herald-weekly as service-binding targets. Atlas's wrangler.jsonc currently has ONE cron (`45 11 * * 1-5`). This plan will expand it to FIVE crons (morning chain + 2 Headhunter + Friday Scout/Herald + Friday 16:30 build). RESEARCH A1 (LOW confidence) flags that the Workers Free plan may cap crons per Worker (historically 3).</what-built>
  <how-to-verify>
    1. From the repo root run: `npx wrangler deploy --dry-run --config apps/atlas/wrangler.jsonc` (or open the Cloudflare dashboard â Workers â atlas â Triggers) AFTER Task 2 stages the expanded crons â OR run a dry-run now with the 5-cron list pasted in temporarily.
    2. Confirm no cron/trigger limit error is returned.
    3. If a limit error appears: decide whether to upgrade to Workers Paid ($5/mo) per build-plan Â§1 (optional headroom), or to consolidate crons. Do NOT seed any secret or flip any flag â this is a plan/infra decision only.
  </how-to-verify>
  <action>Run `npx wrangler deploy --dry-run --config apps/atlas/wrangler.jsonc` (or inspect Cloudflare dashboard → Workers → atlas → Triggers) to confirm the expanded 5-cron Atlas list deploys without a per-Worker trigger-limit error BEFORE Task 2 commits the cron expansion. Pause for owner confirmation; do not let Task 2 expand crons until the cap is confirmed or a Paid upgrade / consolidation path is approved. Infra-verify only — no secret seeding, no flag flips.</action>
  <acceptance_criteria>
    - Owner has confirmed (via dry-run or dashboard) that the 5-cron Atlas list deploys without a trigger-limit error, OR has approved a Workers Paid upgrade / cron consolidation path
  </acceptance_criteria>
  <resume-signal>Type "approved â crons fit" or "approved â upgrading to Paid" or describe the consolidation</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atlas scheduled() â 4 standalone cron cases (dual EDT/EST) + service bindings + crons</name>
  <files>apps/atlas/src/index.ts, apps/atlas/wrangler.jsonc, apps/atlas/test/scheduled.test.ts</files>
  <read_first>
    - apps/atlas/src/index.ts (the existing `switch (controller.cron)` in scheduled() + how the morning-chain case dispatches; the existing dual-form pattern for 45 11 / 45 12)
    - .planning/phases/02-weekly-value/02-PATTERNS.md â `apps/atlas/src/index.ts` section (the EXACT 4 new cases, the Promise.allSettled Friday fan-in, the services + crons extensions, and the THREE landmines: DST dual-case, allSettled not all, cron-count, lines 620-706)
    - .planning/phases/02-weekly-value/02-RESEARCH.md Pattern 6 (standalone cron cases â NOT Workflow steps) + Pitfall 6 (DST drift) + Pitfall 7 (Promise.all vs allSettled)
    - apps/atlas/wrangler.jsonc (the existing services array + triggers.crons to extend)
    - .planning/phases/02-weekly-value/02-CONTEXT.md D2-11 (Friday concurrency build-plan-locked; standalone crons in scheduled(), NOT the morning Workflow)
  </read_first>
  <behavior>
    - Test (scheduled): firing controller.cron `"0 13 * * *"` (or EST `"0 14 * * *"`) calls env.HEADHUNTER.deadlines({date}); `"0 13 * * 1"` calls env.HEADHUNTER.full({date}); `"0 20 * * 5"` calls Promise.allSettled over env.SCOUT.weekly + env.HERALD.weekly (and if Scout rejects, Herald is still invoked â allSettled, not all); `"30 20 * * 5"` calls env.STEWARD.weeklyReviewBuild({date}). The EST dual forms route to the same handlers. Unknown crons fall through to the existing default (no throw).
  </behavior>
  <action>In `apps/atlas/src/index.ts` add 4 dual-case blocks to the `scheduled()` switch (copy the exact shape from PATTERNS apps/atlas/src/index.ts): (1) `case "0 13 * * *": case "0 14 * * *":` â `_ctx.waitUntil(env.HEADHUNTER.deadlines({date}).catch(err â flag P2 kind chain_halted))`; (2) `case "0 13 * * 1": case "0 14 * * 1":` â `env.HEADHUNTER.full({date})`; (3) `case "0 20 * * 5": case "0 21 * * 5":` â `_ctx.waitUntil(Promise.allSettled([env.SCOUT.weekly({date}).catch(flag P2), env.HERALD.weekly({date}).catch(flag P2)]))` (allSettled, NOT all â one failure must not discard the other); (4) `case "30 20 * * 5": case "30 21 * * 5":` â `env.STEWARD.weeklyReviewBuild({date})`. Use `localDate(env)` for date. In `apps/atlas/wrangler.jsonc` add `{ "binding": "SCOUT", "service": "scout" }`, `{ "binding": "HEADHUNTER", "service": "headhunter" }`, `{ "binding": "STEWARD", "service": "steward" }` to the services array, and add the EDT cron forms to `triggers.crons`: `"0 13 * * *"`, `"0 13 * * 1"`, `"0 20 * * 5"`, `"30 20 * * 5"` (keep `"45 11 * * 1-5"`). Annotate each cron with its ET meaning + the EST form to hand-edit at the Nov DST boundary. Keep staging crons []. Create/extend `apps/atlas/test/scheduled.test.ts` to assert each new cron routes to the right RPC and the Friday case uses allSettled.</action>
  <verify>
    <automated>pnpm --filter atlas test -- scheduled</automated>
  </verify>
  <acceptance_criteria>
    - The 4 new cases dispatch standalone RPCs (NOT new MorningChain steps; `grep -c "MORNING_CHAIN.create" apps/atlas/src/index.ts` unchanged from before â no new Workflow step added)
    - The Friday 16:00 case uses `Promise.allSettled` (not `Promise.all`): `grep -c "Promise.allSettled" apps/atlas/src/index.ts` >= 1
    - Both EDT and EST forms are cases in the switch for each new slot (dual-case); wrangler crons use the EDT forms
    - `apps/atlas/wrangler.jsonc` services includes scout, headhunter, steward bindings
    - `pnpm --filter atlas test -- scheduled` exits 0
  </acceptance_criteria>
  <done>Atlas dispatches Headhunter/Scout/Herald/Steward via standalone dual-form cron cases (Friday via allSettled); service bindings + EDT crons added; scheduled test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Steward.weeklyReviewBuild() â compile the 16:30 weekly-review Vault build</name>
  <files>apps/steward/src/index.ts, apps/steward/src/weekly-review.ts, apps/steward/test/weekly-review.test.ts, apps/steward/test/replay.test.ts</files>
  <read_first>
    - apps/steward/src/index.ts (the current default export â fetch() stub + queue consumer; add a WorkerEntrypoint method here without touching the atlas-wire consumer)
    - apps/steward/src/steward.ts (StewardWriter DO + the opâvault_outbox path â the weekly-review build emits a Vault note via the same outbound mechanism)
    - packages/steward-core (the opâLocal-REST map / vault_outbox intent shape the build writes through)
    - .planning/phases/02-weekly-value/02-RESEARCH.md ## Architectural Responsibility Map ("Weekly Vault build (16:30) â Event bus â Steward; Steward compiles") + System Architecture Diagram (the 16:30 build)
    - docs/05-dashboard.md (the weekly-review build layout â read on demand for the section set)
    - .planning/phases/02-weekly-value/02-06-SUMMARY.md (the herald:weekly digest event the build reads) + 02-04-SUMMARY.md (the scout events the build reads)
    - apps/steward/test/replay.test.ts (the existing op:"increment"/entity:"tasks" replay-idempotency test — EXTEND with an op:"upsert"/entity:"flag" case; keep the existing case intact)
  </read_first>
  <behavior>
    - Test (weekly-review): weeklyReviewBuild({date}) compiles a weekly-review note from the week's Scout events + Herald weekly digest + funnel/flag state and writes it via the existing vault_outbox path (NOT a new writer â Steward stays the sole Vault writer). Re-running for the same date is idempotent (the build REPLACES the weekly note, like compass:plan:<date> replaces Today). It must NOT add a second atlas-wire consumer.
  </behavior>
  <action>Create `apps/steward/src/weekly-review.ts` exporting `weeklyReview(env, date)` that compiles the 16:30 weekly-review note (the docs/05-dashboard.md weekly-review section set: the week's surfaced events, the Herald week-in-review digest, the jobs funnel snapshot, open flags) and enqueues ONE vault_outbox intent via the existing steward-core opâLocal-REST path (op:"upsert" semantics â REPLACE the weekly note for that date, idempotent like compass:plan). In `apps/steward/src/index.ts` expose a WorkerEntrypoint method `async weeklyReviewBuild(params?: { date? }): Promise<void>` delegating to `weeklyReview(this.env, params?.date ?? localDate(this.env))`. Do NOT modify the existing `atlas-wire` queue consumer (Steward remains the sole consumer). Create `apps/steward/test/weekly-review.test.ts`. Additionally EXTEND `apps/steward/test/replay.test.ts` with a replay-idempotency case for the canonical `op:"upsert"`/`entity:"flag"` event (idempotencyKey `flg:<date>:<agent>:<hash>`): applying the same flag event twice leaves the flag row unchanged (second `apply()` returns `{ applied: false }`, `meta.changes === 0`) — the CLAUDE.md Definition-of-Done replay test for the flag-upsert shape Flagger (02-02) and the funnel upserts (02-05) emit.</action>
  <verify>
    <automated>pnpm --filter steward test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/steward/src/index.ts` exposes `weeklyReviewBuild()` as a WorkerEntrypoint RPC method (callable as env.STEWARD.weeklyReviewBuild)
    - The build writes via the existing vault_outbox / steward-core path (no new Vault writer; `grep -rn "queues.consumers" apps/steward/wrangler.jsonc | grep atlas-wire` still shows steward as the SOLE atlas-wire consumer)
    - Re-running for the same date REPLACES the weekly note (idempotent), not appends
    - `apps/steward/test/replay.test.ts` contains an `op:"upsert"`/`entity:"flag"` replay case proving a second apply is a no-op (`{ applied: false }`, flag row/count unchanged)
    - `pnpm --filter steward test` exits 0 (weekly-review + the extended flag-upsert replay case both green)
  </acceptance_criteria>
  <done>Steward.weeklyReviewBuild compiles the 16:30 weekly-review note via the existing vault path, idempotent; Steward stays sole atlas-wire consumer; weekly-review test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron trigger â Atlas scheduled() | Cloudflare-internal trigger; no external input |
| Atlas â Scout/Headhunter/Herald/Steward (service RPC) | Internal Worker-to-Worker RPC (D-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-cron1 | DoS | wrong DST cron form fires the Friday build at the wrong hour | mitigate | EDT form in wrangler now (active June); dual EDT/EST cases in the switch; annotated hand-edit at Nov boundary; /cron-utc to verify |
| T-02-cron2 | DoS | Friday Scout failure discards Herald's completed work | mitigate | Promise.allSettled (not all) â each wrapped in catchâflag; the 16:30 build runs on a partial summary, never blocked |
| T-02-cron3 | DoS | exceeding the Free per-Worker cron cap silently fails deploy | mitigate | Blocking human-verify checkpoint (Task 1) before expanding crons; upgrade to Paid only if capped |
| T-02-wr | Spoofing/Tampering | weekly-review build introduces a second Vault writer / atlas-wire consumer | mitigate | Build writes via existing vault_outbox; acceptance asserts Steward remains the sole atlas-wire consumer (Pillar 1) |
| T-02-SC | Tampering | npm installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `pnpm --filter atlas test -- scheduled` and `pnpm --filter steward test -- weekly-review` exit 0
- `grep -c "Promise.allSettled" apps/atlas/src/index.ts` >= 1 (Friday fan-in)
- `grep -rn "queues.consumers" apps/*/wrangler.jsonc | grep atlas-wire` â only apps/steward
- `pnpm test` full suite green
- (Owner gate, NOT code) cron-cap human-verify approved before deploy
</verification>

<success_criteria>
Atlas dispatches Phase-2 cadence via 4 standalone dual-form cron cases (Friday via allSettled); Scout/Headhunter/Steward service bindings + EDT crons added; Steward compiles the 16:30 weekly-review build idempotently; cron-cap checkpoint cleared; Steward stays sole atlas-wire consumer; full suite green.
</success_criteria>

<output>
Create `.planning/phases/02-weekly-value/02-07-SUMMARY.md` when done
</output>
