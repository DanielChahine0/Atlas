---
phase: 02-weekly-value
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/headhunter/src/index.ts
  - apps/headhunter/src/windows.ts
  - apps/headhunter/src/state.ts
  - apps/headhunter/src/seed.ts
  - apps/headhunter/wrangler.jsonc
  - apps/headhunter/package.json
  - apps/headhunter/vitest.config.ts
  - apps/headhunter/test/idempotent.test.ts
  - apps/headhunter/test/low-confidence.test.ts
  - apps/headhunter/test/urgency.test.ts
  - apps/headhunter/test/funnel.test.ts
autonomous: true
requirements: [WEEKLY-01]
user_setup:
  - service: headhunter-config
    why: "Owner-curated watchlist/boards/cycle KV (D2-15) — Phase-2 go-live config gate; a small starter seed ships in code so it runs out of the box"
    env_vars: []
    dashboard_config:
      - task: "Seed CONFIG headhunter/tracked_companies, headhunter/boards, headhunter/cycle, headhunter/rolling_companies before go-live"
        location: "Cloudflare KV CONFIG namespace"
must_haves:
  truths:
    - "Headhunter.full (Mon) scans boards + advances the per-company window state machine; deadlines (daily-light) promotes closing windows"
    - "Apply-by tasks are created THROUGH Forge's path (env.FORGE.createTask), never by writing the tasks table directly (D2-13/D2-14)"
    - "Urgency (window inside lead-time OR explicit deadline) bypasses the fit_floor — nothing time-critical is hidden by a fit score (D2-14)"
    - "A low-confidence historical window date routes to a Flagger P3 (kind low_confidence_window), NOT silently to a task (D2-14)"
    - "Funnel counts come from Filer Type/Job threads, Headhunter is the single emitter, deduped by (thread, stage) (D2-13)"
    - "Re-running a scan leaves pipeline counts unchanged — same window → same task, no counter inflation (GATING)"
  artifacts:
    - path: "apps/headhunter/src/index.ts"
      provides: "Headhunter WorkerEntrypoint full() + deadlines()"
      exports: ["Headhunter", "default"]
    - path: "apps/headhunter/src/windows.ts"
      provides: "window state machine + funnel stage classification"
      contains: "upcoming"
    - path: "apps/headhunter/src/state.ts"
      provides: "HeadhunterState DO (serialized window transitions)"
      contains: "class HeadhunterState"
  key_links:
    - from: "apps/headhunter/src/index.ts"
      to: "Forge"
      via: "env.FORGE.createTask service binding"
      pattern: "FORGE.createTask"
    - from: "apps/headhunter/src/index.ts"
      to: "atlas-wire"
      via: "funnel increment + scan summary upsert"
      pattern: "send\\(env"
    - from: "apps/headhunter/src/windows.ts"
      to: "D1 windows/jobs"
      via: "INSERT OR REPLACE"
      pattern: "INTO windows"
---

<objective>
Build Headhunter (#8): the hiring-window + deadline tracker. `full` (Mon 09:00) scans boards and advances a per-company/cycle window state machine in D1 (`windows`/`jobs`) + `HeadhunterState` DO; `deadlines` (daily-light 09:00) promotes closing windows and emits apply-by tasks THROUGH Forge's existing dedupe path (`env.FORGE.createTask`, never a new store). Urgency bypasses the fit floor (D2-14); low-confidence window dates route to a Flagger P3 (never a silent task). The job-pipeline funnel is driven by Filer `Type/Job` threads with Headhunter as the single emitter, deduped by `(thread, stage)` (D2-13). Read/track only — Headhunter never applies.

Purpose: WEEKLY-01's jobs half. Hard edge: Headhunter feeds Forge. Reuses `packages/tasks` dedupe + Codex fit ranking.
Output: `apps/headhunter` Worker + HeadhunterState DO + windows/funnel logic + starter seed + idempotent/low-confidence/urgency/funnel tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-weekly-value/02-CONTEXT.md
@.planning/phases/02-weekly-value/02-RESEARCH.md
@.planning/phases/02-weekly-value/02-PATTERNS.md
@.planning/phases/02-weekly-value/02-VALIDATION.md
@.planning/phases/02-weekly-value/02-01-SUMMARY.md

<interfaces>
From @atlas/shared (after 02-01): `localDate(env?)`, `contentHash`, `flag(env, sev, title, detail?, { sourceAgent, kind, runId })` (→ INCIDENTS), `Severity`.
From @atlas/wire: `send(env, event)`, `WireEvent`.
From @atlas/codex: `read()` (skills/projects/addresses for fit ranking; read-only, D2-12).
Forge service binding (D-11 RPC, see RESEARCH Code Example): `env.FORGE.createTask({ title, due, due_kind, source_agent, thread, priority }, { idempotencyKey, runId })`. Forge's existing dedupe is `sha256(thread+normalizedTitle+dueDate)` — the apply-by task's idempotencyKey is `headhunter:window:<company>:<cycle>`. Confirm the exact createTask signature by reading apps/forge/src/index.ts (class Forge ~line 239) before calling.

D1 tables (from 02-01 migration 0004): `windows(id, company, cycle, role_class, opens_est, closes_est, confidence REAL, source, status, last_seen_open, created_at, updated_at)`; `jobs(id, company, title_norm, location_norm, cycle, window_id, sources, deadline, fit_score REAL, status, first_seen, last_seen)` with UNIQUE idx_jobs_fingerprint. Positional `?` only.

Window state machine: upcoming → open → closing → closed. lead_time_days default 21; fit_floor 0.4. Fingerprint: djb2(normalize(company)+normalize(title)+location+cycle).
Idempotency keys: apply-by task `headhunter:window:<company>:<cycle>`; funnel increment `headhunter:funnel:<thread_id>:<stage>`; scan summary `headhunter:scan:<date>`.
Filer Job/* sub-labels for stage classification (D2-13): Job/OA, Job/Interview, Job/Offer, Job/Rejected (read back via gmail.readonly on Type/Job threads).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HeadhunterState DO + window state machine + D1 windows/jobs (idempotent + low-confidence + urgency)</name>
  <files>apps/headhunter/src/state.ts, apps/headhunter/src/windows.ts, apps/headhunter/src/seed.ts, apps/headhunter/package.json, apps/headhunter/vitest.config.ts, apps/headhunter/test/idempotent.test.ts, apps/headhunter/test/low-confidence.test.ts, apps/headhunter/test/urgency.test.ts</files>
  <read_first>
    - .planning/phases/02-weekly-value/02-PATTERNS.md → `apps/headhunter/src/state.ts` section (HeadhunterState DO + blockConcurrencyWhile + new_sqlite_classes, lines 218-251) + `apps/headhunter/src/windows.ts` analog + Shared Pattern "blockConcurrencyWhile error capture" + "D1 positional ?"
    - apps/steward/src/steward.ts lines 42-56 (blockConcurrencyWhile: capture error inside the gate, re-throw after it closes — callback must NEVER throw)
    - .planning/phases/02-weekly-value/02-RESEARCH.md Pattern 8 (windows/jobs DDL, fingerprint, HeadhunterState singleton) + "D2-14 urgency bypass" code example (lines 711-722)
    - .planning/phases/02-weekly-value/02-CONTEXT.md D2-14 (urgency bypasses fit floor; low-confidence date → P3) + D2-15 (KV watchlist + starter seed)
    - migrations/0004_incidents_flagger.sql (the windows/jobs columns to bind — created in 02-01)
    - apps/dlq-sink/src/index.ts lines 126-143 (D1 positional ? INSERT OR REPLACE)
  </read_first>
  <behavior>
    - Test (idempotent): advancing the same window twice (same company/cycle) keeps ONE windows row and yields the SAME apply-by task idempotencyKey `headhunter:window:<co>:<cycle>` — no counter inflation; re-scan produces the same jobs rows (UNIQUE fingerprint) (GATING).
    - Test (low-confidence): a window with `confidence < 0.4 && source === "historical"` calls flag(env,"P3",...,{kind:"low_confidence_window"}) and does NOT create an apply-by task.
    - Test (urgency): a window inside lead_time_days (default 21) OR with an explicit deadline, even with fit_score below fit_floor (0.4), STILL produces an apply-by task (urgency bypasses the fit floor); a non-urgent below-floor window does NOT.
  </behavior>
  <action>Create `apps/headhunter/package.json` (name "headhunter"; deps @atlas/shared, @atlas/wire, @atlas/codex, vitest; rss-parser only if a board uses RSS). Create `apps/headhunter/src/state.ts` exporting `class HeadhunterState extends DurableObject<Env>` addressed as `env.HEADHUNTER_STATE.getByName("pipeline")` with `advanceWindow(windowId, newStatus)` using `blockConcurrencyWhile` (capture-inside-gate, re-throw-after) to serialize concurrent full/deadlines runs on the same window row, and `last_seen_open` advancement. Create `apps/headhunter/src/windows.ts` with: the fingerprint helper (djb2 of normalize(company)+normalize(title)+location+cycle), the state machine `upcoming→open→closing→closed`, `INSERT OR REPLACE INTO windows(...)` / `INTO jobs(...)` via positional `?`, an `isUrgent(window)` predicate (inside lead_time_days OR explicit deadline), and the decision branch: if `confidence < 0.4 && source==="historical"` → `flag(env,"P3", "Low-confidence window: <co> <cycle>", detail, { sourceAgent:"Headhunter", kind:"low_confidence_window", runId })` and return WITHOUT a task; else if urgent OR `fit_score >= fit_floor` → return a task descriptor for Task 2 to emit via Forge. Create `apps/headhunter/src/seed.ts` with the small D2-15 starter seed (a few well-known intern/new-grad programs + the `fall-2026` cycle) loaded only when CONFIG `headhunter/tracked_companies` is unset. The root vitest.workspace.ts uses a glob (apps/*/vitest.config.ts) and auto-discovers the new app once its vitest.config.ts exists — do NOT edit vitest.workspace.ts.</action>
  <verify>
    <automated>pnpm --filter headhunter test -- idempotent low-confidence urgency</automated>
  </verify>
  <acceptance_criteria>
    - `apps/headhunter/src/state.ts` declares `class HeadhunterState extends DurableObject<Env>` and uses `blockConcurrencyWhile` with error capture inside the gate (callback never throws)
    - Re-scanning the same window keeps ONE windows row and one stable task key `headhunter:window:<co>:<cycle>` (idempotent test, GATING)
    - A low-confidence historical window emits a P3 flag (kind low_confidence_window) and creates NO task (low-confidence test)
    - Urgency (in lead-time / explicit deadline) bypasses fit_floor → still tasks (urgency test)
    - D1 writes use positional `?` only; UNIQUE idx_jobs_fingerprint relied on for dedupe
    - `pnpm --filter headhunter test -- idempotent low-confidence urgency` exits 0
  </acceptance_criteria>
  <done>HeadhunterState DO + window state machine + D1 persistence; idempotent (GATING) + low-confidence + urgency tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Headhunter full()/deadlines() entrypoints — Forge tasks, funnel single-emitter, wrangler</name>
  <files>apps/headhunter/src/index.ts, apps/headhunter/wrangler.jsonc, apps/headhunter/test/funnel.test.ts</files>
  <read_first>
    - .planning/phases/02-weekly-value/02-PATTERNS.md → `apps/headhunter/src/index.ts` section (two-mode WorkerEntrypoint full()/deadlines() + idempotency keys + D1 positional ? + the Forge service-binding landmine, lines 177-214)
    - apps/compass/src/index.ts (WorkerEntrypoint + scheduled modes analog)
    - apps/forge/src/index.ts (class Forge ~line 239 — confirm the exact createTask RPC signature before calling env.FORGE.createTask)
    - .planning/phases/02-weekly-value/02-RESEARCH.md Code Example "Headhunter task emission through Forge path" (lines 903-923) + "Open Questions" #3 (service-binding RPC, not a new Wire event)
    - .planning/phases/02-weekly-value/02-CONTEXT.md D2-13 (funnel from Filer Type/Job threads; single emitter; dedupe by (thread,stage))
    - apps/headhunter/wrangler.jsonc analog: apps/compass/wrangler.jsonc + apps/steward/wrangler.jsonc (DO binding) — needs services FORGE
  </read_first>
  <behavior>
    - Test (funnel): given two Filer Type/Job threads classified to stages (e.g. thread A → "oa", thread B → "interview"), Headhunter emits one funnel increment per (thread, stage) with idempotencyKey `headhunter:funnel:<thread>:<stage>`; re-processing the same threads emits the SAME keys (replay-safe — Steward dedups, meta.changes===0). Headhunter is the ONLY emitter of funnel increments (single-emitter, no double-count).
  </behavior>
  <action>Create `apps/headhunter/src/index.ts`: `export class Headhunter extends WorkerEntrypoint<Env>` with `async full(params?: { date? })` (runFull: scan boards → upsert jobs/windows via windows.ts → advance state machine via HeadhunterState DO → classify Filer Type/Job threads to funnel stages → emit funnel increments + apply-by tasks via Forge → emit a `headhunter:scan:<date>` summary upsert) and `async deadlines(params?: { date? })` (runDeadlines: promote closing windows, emit apply-by tasks for closing windows). Apply-by tasks go through `env.FORGE.createTask({ title: \`apply by <closes_est> — <company> <role_class> (<cycle>)\`, due: closes_est, due_kind: confidence>=0.7?"explicit":"inferred", source_agent:"Headhunter", thread: null, priority: urgency?"P1":"P2" }, { idempotencyKey: \`headhunter:window:<co>:<cycle>\`, runId: date })` — NEVER write the tasks table directly. Funnel increments use `send()` with idempotencyKey `headhunter:funnel:<thread>:<stage>` (op:"increment"), single-emitter per D2-13. On failure flag(env,"P2",...,{sourceAgent:"Headhunter", kind:"headhunter_failed"}). Add a `kind:"heartbeat"` incident emit at the end of a successful run. Create `apps/headhunter/wrangler.jsonc`: producers WIRE + INCIDENTS; durable_objects HEADHUNTER_STATE; migrations new_sqlite_classes ["HeadhunterState"]; D1 DB; CONFIG KV; AI binding; `services` includes `{ "binding":"FORGE", "service":"forge" }`; nodejs_compat; NO own cron (Atlas drives via service binding in Plan 02-07); staging crons []; NO consumers block.</action>
  <verify>
    <automated>pnpm --filter headhunter test -- funnel</automated>
  </verify>
  <acceptance_criteria>
    - `apps/headhunter/src/index.ts` exports `class Headhunter extends WorkerEntrypoint<Env>` with `full()` and `deadlines()` and a `scheduled()`/RPC-driven default that `satisfies ExportedHandler<Env>`
    - Apply-by tasks are created via `env.FORGE.createTask(...)` (grep: `grep -c "FORGE.createTask" apps/headhunter/src/index.ts` >= 1); the tasks table is NEVER written directly (`grep -c "INTO tasks" apps/headhunter/src/index.ts` == 0)
    - Funnel increment idempotencyKey matches `headhunter:funnel:<thread>:<stage>`; Headhunter is the single emitter (funnel test)
    - `apps/headhunter/wrangler.jsonc` declares the FORGE service binding, the HEADHUNTER_STATE DO, NO own cron, NO consumers block
    - `pnpm --filter headhunter test -- funnel` exits 0
  </acceptance_criteria>
  <done>Headhunter full()/deadlines() emit apply-by tasks via Forge + single-emitter funnel increments; wrangler wired (Forge binding, DO, no cron); funnel test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public job boards → Headhunter | Untrusted scraped content (read-only, no link-following beyond declared boards) |
| Filer Type/Job email → Headhunter | Email-sourced stage classification (read-only, gmail.readonly) |
| Headhunter → Forge (service RPC) | Internal RPC; Forge owns the task dedupe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-hh1 | Tampering | double-counted funnel (two emitters) | mitigate | D2-13 single emitter (Headhunter only); dedupe by (thread,stage) idempotencyKey; Steward replay no-op (meta.changes===0) |
| T-02-hh2 | Repudiation | a shaky window date silently becomes a task | mitigate | D2-14: low-confidence historical date → P3 flag, never a silent task; urgency bypass only sets task priority, not date trust |
| T-02-cred | Information Disclosure | scraping credentials leak | mitigate | Read-only OAuth scopes; no board credentials stored in KV/Vault/[vars]; all secrets via Secrets Store; public boards only |
| T-02-hh3 | Spoofing/Tampering | Headhunter writes the tasks table directly (bypassing Forge dedupe) | mitigate | Apply-by tasks ONLY via env.FORGE.createTask; acceptance asserts no `INTO tasks` in headhunter src |
| T-02-SC | Tampering | npm installs | mitigate | Only rss-parser if a board needs RSS (RESEARCH audit [OK]/Approved); no [ASSUMED]/[SUS] → no blocking checkpoint |
</threat_model>

<verification>
- `pnpm --filter headhunter test` exits 0 (idempotent GATING + low-confidence + urgency + funnel)
- `grep -rn "queues.consumers" apps/headhunter/wrangler.jsonc` → empty
- Definition-of-Done: Wire-contract test (funnel increment + scan summary shape) + replay (Steward meta.changes===0) + failure-path (headhunter_failed P2) present
- `pnpm test` full suite green
</verification>

<success_criteria>
Headhunter tracks hiring windows in D1, emits apply-by tasks via Forge (urgency bypasses fit floor; low-confidence → P3, not a task), drives the funnel from Filer Type/Job threads as the single emitter; re-scan idempotent (GATING); all four tests green.
</success_criteria>

<output>
Create `.planning/phases/02-weekly-value/02-05-SUMMARY.md` when done
</output>
