---
phase: 02-weekly-value
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/flag.ts
  - packages/shared/src/incident.ts
  - packages/shared/src/env.ts
  - packages/shared/src/index.ts
  - packages/shared/test/flag.test.ts
  - migrations/0004_incidents_flagger.sql
  - apps/dlq-sink/src/index.ts
  - apps/dlq-sink/wrangler.jsonc
  - apps/dlq-sink/test/dlq.test.ts
  - packages/model/src/claude.ts
  - apps/steward/src/steward-consumer.ts
  - apps/steward/wrangler.jsonc
  - apps/atlas/src/coordinator.ts
  - apps/atlas/src/morning-chain.ts
  - apps/atlas/src/index.ts
  - apps/atlas/wrangler.jsonc
  - apps/herald/src/guardrail.ts
  - apps/herald/wrangler.jsonc
  - apps/forge/src/index.ts
  - apps/forge/wrangler.jsonc
  - apps/sundial/src/reconcile.ts
  - apps/sundial/wrangler.jsonc
  - apps/compass/src/index.ts
  - apps/compass/wrangler.jsonc
  - apps/filer/src/index.ts
  - apps/filer/wrangler.jsonc
  - packages/security/src/redact.ts
autonomous: true
requirements: [WEEKLY-02]
must_haves:
  truths:
    - "flag() enqueues a RawIncident onto atlas-incidents (INCIDENTS binding), never emits a finished flag to atlas-wire"
    - "All ~15 existing flag() call sites compile and pass a kind tag"
    - "The full suite stays green (>=315 tests) after the rework"
    - "migration 0004 creates events, windows, jobs, and flags tables on atlas-db"
    - "Steward remains the SOLE atlas-wire consumer (no new consumer added by this plan)"
  artifacts:
    - path: "packages/shared/src/incident.ts"
      provides: "RawIncident zod schema + type"
      contains: "RawIncident"
    - path: "packages/shared/src/flag.ts"
      provides: "reworked flag() targeting INCIDENTS queue"
      contains: "INCIDENTS"
    - path: "migrations/0004_incidents_flagger.sql"
      provides: "events/windows/jobs/flags D1 tables"
      contains: "CREATE TABLE IF NOT EXISTS events"
  key_links:
    - from: "packages/shared/src/flag.ts"
      to: "env.INCIDENTS"
      via: "Queue.send(RawIncident)"
      pattern: "INCIDENTS\\.send"
    - from: "every Phase-0/1 wrangler.jsonc"
      to: "atlas-incidents"
      via: "INCIDENTS producer binding"
      pattern: "atlas-incidents"
---

<objective>
Rework the cross-cutting incident substrate so Flagger can later consume it. Introduce the `RawIncident` schema, provision the new `atlas-incidents` queue topology (D2-04), add the `INCIDENTS` producer binding to every Phase-0/1 Worker, rework `flag()` (D2-05) to enqueue a raw incident onto the new `atlas-incidents` queue instead of emitting a finished `FlagRecord` to `atlas-wire`, migrate ALL existing `flag()` call sites, and create the `0004` D1 migration (events/windows/jobs/flags) that Scout/Headhunter/Flagger consume.

Purpose: This is the hard edge of the Phase-2 retrofit. Until `flag()` targets `atlas-incidents`, Flagger has nothing to consume. It MUST land first and keep the 315 existing tests green.
Output: `RawIncident` type + schema, reworked `flag()`, migrated callers, `0004` migration, every wrangler gains an `INCIDENTS` producer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-weekly-value/02-CONTEXT.md
@.planning/phases/02-weekly-value/02-RESEARCH.md
@.planning/phases/02-weekly-value/02-PATTERNS.md
@.planning/phases/02-weekly-value/02-VALIDATION.md

<interfaces>
<!-- Current flag() signature being reworked (packages/shared/src/flag.ts) -->
Current: `flag(env: { WIRE: Queue<WireEvent> }, severity: Severity, title: string, detail?: string, options: FlagOptions = {})`
- builds a full FlagRecord with DEFAULT_TRUST + contentHash djb2 id, emits `{type:"flag", entity:"flag", op:"upsert", idempotencyKey: id}` via @atlas/wire send().

Target (D2-05): `flag(env: { INCIDENTS: Queue<RawIncident> }, severity, title, detail?, options: FlagOptions & { kind?: string; runId?: string } = {})`
- builds a RawIncident `{ source_agent, kind, severity_hint, title, detail?, run_id? }` and calls `env.INCIDENTS.send(incident)`.

Current shared Env (packages/shared/src/env.ts): exports `Severity = "P1"|"P2"|"P3"|"P4"` and `interface Env { WIRE; DB; CONFIG; OAUTH_KV?; BLOBS?; AI?; STEWARD_LOCK?; ATLAS?; ... MODEL_<CODENAME> }`.

`localDate(_env?, now?)` and `contentHash` (djb2) already live in flag.ts. `contentHash` / `DEFAULT_TRUST` / `FlagRecord` MOVE to Flagger (Plan 02-02) — but to avoid a cross-plan dependency this plan KEEPS `FlagRecord` exported from @atlas/shared (it becomes Flagger's output shape) and KEEPS `contentHash`/`localDate` exported (reused everywhere). Only the emit BODY of flag() changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RawIncident schema + INCIDENTS binding to shared, rework flag()</name>
  <files>packages/shared/src/incident.ts, packages/shared/src/flag.ts, packages/shared/src/env.ts, packages/shared/src/index.ts, packages/shared/test/flag.test.ts</files>
  <read_first>
    - packages/shared/src/flag.ts (the helper being reworked — full current body in <interfaces> above)
    - packages/shared/src/env.ts (the shared Env + Severity union to extend)
    - packages/shared/src/index.ts (the barrel — confirm what is re-exported)
    - packages/shared/test/flag.test.ts (the existing assertions that break — must be rewritten to assert RawIncident on INCIDENTS, not the §6.4 flag event)
    - packages/wire/src/send.ts (Queue.send() producer pattern; flag() no longer uses send() — it calls env.INCIDENTS.send() directly)
    - .planning/phases/02-weekly-value/02-RESEARCH.md ## Code Examples "Reworked flag()" (the exact target body, lines 830-866) + Pattern 3
    - .planning/phases/02-weekly-value/02-PATTERNS.md → `packages/shared/src/flag.ts` section (target impl + migration surface table)
  </read_first>
  <behavior>
    - Test 1: flag(env, "P2", "x", "y", { sourceAgent: "Forge", kind: "model_error" }) calls env.INCIDENTS.send with `{ source_agent: "Forge", kind: "model_error", severity_hint: "P2", title: "x", detail: "y" }` and NOT env.WIRE.send.
    - Test 2: omitted kind defaults to "unknown"; omitted sourceAgent defaults to "Atlas".
    - Test 3: runId option is passed through as run_id when provided, absent otherwise.
    - Test 4: RawIncident.parse rejects an object missing source_agent/kind/severity_hint/title (zod).
  </behavior>
  <action>Create `packages/shared/src/incident.ts` exporting a zod schema `RawIncidentSchema` and `type RawIncident = { source_agent: string; kind: string; severity_hint: Severity; title: string; detail?: string; run_id?: string }`. Use `z.object({...})` with `severity_hint: z.enum(["P1","P2","P3","P4"])`. In `env.ts`, add `INCIDENTS?: Queue<RawIncident>;` to `interface Env` (optional so any not-yet-retrofitted Worker still compiles) and import the type. Rework `flag()` in `flag.ts`: change the env param type to `{ INCIDENTS: Queue<RawIncident> }`, extend `FlagOptions` with `kind?: string` and `runId?: string`, build a `RawIncident` (`source_agent: options.sourceAgent ?? "Atlas"`, `kind: options.kind ?? "unknown"`, `severity_hint: severity`, `title`, `detail`, `run_id: options.runId`), and call `await env.INCIDENTS.send(incident)`. KEEP `localDate`, `contentHash`, `DEFAULT_TRUST`, and `FlagRecord` exported (Flagger reuses them). Re-export `RawIncident`/`RawIncidentSchema` from `packages/shared/src/index.ts`. Rewrite `packages/shared/test/flag.test.ts` to assert the RawIncident shape on a mock `INCIDENTS` queue (drop the old §6.4 flag-event assertions).</action>
  <verify>
    <automated>pnpm --filter @atlas/shared test</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/flag.ts` no longer references `env.WIRE` or calls `send()`; it calls `env.INCIDENTS.send(...)` (grep: `grep -c "INCIDENTS.send" packages/shared/src/flag.ts` >= 1; `grep -c "env.WIRE" packages/shared/src/flag.ts` == 0)
    - `packages/shared/src/incident.ts` exports `RawIncident` type + `RawIncidentSchema` zod schema
    - `packages/shared/src/env.ts` declares `INCIDENTS?: Queue<RawIncident>`
    - `pnpm --filter @atlas/shared test` exits 0
  </acceptance_criteria>
  <done>RawIncident schema exists; flag() enqueues to INCIDENTS; shared package tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all ~15 flag() call sites + add INCIDENTS producer to every wrangler</name>
  <files>apps/dlq-sink/src/index.ts, apps/dlq-sink/wrangler.jsonc, apps/dlq-sink/test/dlq.test.ts, packages/model/src/claude.ts, apps/steward/src/steward-consumer.ts, apps/steward/wrangler.jsonc, apps/atlas/src/coordinator.ts, apps/atlas/src/morning-chain.ts, apps/atlas/src/index.ts, apps/atlas/wrangler.jsonc, apps/herald/src/guardrail.ts, apps/herald/wrangler.jsonc, apps/forge/src/index.ts, apps/forge/wrangler.jsonc, apps/sundial/src/reconcile.ts, apps/sundial/wrangler.jsonc, apps/compass/src/index.ts, apps/compass/wrangler.jsonc, apps/filer/src/index.ts, apps/filer/wrangler.jsonc, packages/security/src/redact.ts</files>
  <read_first>
    - .planning/phases/02-weekly-value/02-PATTERNS.md → `packages/shared/src/flag.ts` section "Migration surface (all callers...)" table (the exact kind tag per file)
    - .planning/phases/02-weekly-value/02-RESEARCH.md Pattern 3 "Full caller list (verified by grep)" table
    - apps/dlq-sink/src/index.ts line ~148 (flag call), apps/dlq-sink/wrangler.jsonc (producer block to extend), apps/dlq-sink/test/dlq.test.ts (assertions to update to RawIncident)
    - packages/model/src/claude.ts lines 122, 266, 283 (three flag sites; kind "model_error")
    - apps/steward/src/steward-consumer.ts line ~67 (malformed wire event; kind "malformed_event")
    - apps/atlas/src/coordinator.ts line ~91, apps/atlas/src/morning-chain.ts line ~149, apps/atlas/src/index.ts line ~136
    - apps/herald/src/guardrail.ts line ~38 (kind "security_leak_blocked")
    - apps/forge/src/index.ts line ~127, apps/sundial/src/reconcile.ts lines ~109/~156, apps/compass/src/index.ts line ~114, apps/filer/src/index.ts line ~165
    - packages/security/src/redact.ts line ~12 (the P1 "secret-exposure-blocked" path; kind "security_leak_blocked")
    - apps/compass/wrangler.jsonc (canonical producers block shape — `{ "binding": "WIRE", "queue": "atlas-wire" }`; add the INCIDENTS sibling)
  </read_first>
  <action>For each file with a `flag(env, ...)` call (dlq-sink x1, model/claude x3, steward-consumer x1, atlas/coordinator x1, atlas/morning-chain x1, atlas/index x1, herald/guardrail x1, forge x1, sundial/reconcile x2, compass x1, filer x1, security/redact x1) add the `kind:` tag to the options object per the PATTERNS migration table (dlq-sink → `"dlq_dead_letter"`; model → `"model_error"`; steward → `"malformed_event"`; coordinator → `"heartbeat_stale"`; morning-chain → `"chain_halted"`; atlas/index → `"workflow_create_failed"`; herald → `"security_leak_blocked"`; forge → per its current intent e.g. `"phishing_skipped"`; sundial → `"calendar_sync_failed"`; compass → `"overcommit"`; filer → `"watch_renewal_due"`; security/redact → `"security_leak_blocked"`). Add `{ "binding": "INCIDENTS", "queue": "atlas-incidents" }` to the `queues.producers` array in EACH of these wrangler.jsonc files: dlq-sink, steward, atlas, herald, forge, sundial, compass, filer (the producers block already exists with the WIRE binding). Do NOT add a `consumers` block for atlas-incidents anywhere except where Plan 02-02 will (Flagger) — this plan adds producers only. Update `apps/dlq-sink/test/dlq.test.ts` so the assertion checks the RawIncident enqueued to INCIDENTS (kind "dlq_dead_letter") instead of the old §6.4 flag event on WIRE. NOTE: the security/redact.ts P1 path and any env types that flag() touches must expose INCIDENTS — rely on the optional `INCIDENTS?` already added to shared Env.</action>
  <verify>
    <automated>pnpm -r typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "atlas-incidents" apps/*/wrangler.jsonc | wc -l` shows the INCIDENTS producer present in dlq-sink, steward, atlas, herald, forge, sundial, compass, filer wrangler files (>=8)
    - `grep -rn "queues.consumers" apps/*/wrangler.jsonc | grep -c atlas-incidents` == 0 (no consumer added by this plan)
    - `grep -rn "queues.consumers" apps/*/wrangler.jsonc | grep atlas-wire` shows atlas-wire consumer ONLY in apps/steward (Pillar 1 intact)
    - Every migrated `flag(env, ...)` call passes a `kind` in its options object (no call left without a kind tag among the listed files)
    - `pnpm -r typecheck` exits 0 and `pnpm test` exits 0 (>=315 tests, no regression)
  </acceptance_criteria>
  <done>All flag() callers migrated with kind tags; every Phase-0/1 wrangler has the INCIDENTS producer; full suite green; Steward still the sole atlas-wire consumer.</done>
</task>

<task type="auto">
  <name>Task 3: Create 0004 D1 migration (events, windows, jobs, flags tables)</name>
  <files>migrations/0004_incidents_flagger.sql</files>
  <read_first>
    - migrations/0003_tasks.sql (the existing migration style — CREATE TABLE IF NOT EXISTS + CREATE INDEX; positional/no named params; column naming conventions)
    - migrations/0001_init_core.sql (counters/idempotency_keys/audit_log/run_log table style for consistency)
    - .planning/phases/02-weekly-value/02-RESEARCH.md Pattern 7 (the `events` table DDL) + Pattern 8 (the `windows` + `jobs` table DDL)
    - .planning/phases/02-weekly-value/02-RESEARCH.md Assumptions A3 (FlaggerState DO SQLite holds live flag state; a D1 `flags` table is optional/audit — include a minimal `flags` audit table here so Flagger can persist resolved-flag history without a later migration)
  </read_first>
  <action>Create `migrations/0004_incidents_flagger.sql` with four `CREATE TABLE IF NOT EXISTS` blocks plus their indexes, copying the exact column sets from RESEARCH Pattern 7 (`events`: id TEXT PK, title, start, end, location, online INTEGER, url, source, relevance INTEGER, why, horizon, status, digest_date, created_at INTEGER; indexes idx_events_start, idx_events_status) and Pattern 8 (`windows`: id TEXT PK, company, cycle, role_class, opens_est, closes_est, confidence REAL, source, status, last_seen_open, created_at INTEGER, updated_at INTEGER; `jobs`: id TEXT PK, company, title_norm, location_norm, cycle, window_id, sources TEXT default '[]', deadline, fit_score REAL, status, first_seen, last_seen; indexes idx_jobs_fingerprint UNIQUE, idx_jobs_window, idx_jobs_status). Add a `flags` audit table: id TEXT PRIMARY KEY, signature TEXT, source_agent TEXT, severity TEXT, trust INTEGER, status TEXT, title TEXT, detail TEXT, recurrence INTEGER DEFAULT 1, created_at INTEGER, updated_at INTEGER; index idx_flags_severity on (severity, trust). Use anonymous positional convention (no named params), SQLite types only. Do NOT apply the migration here (executor/CI applies via vitest applyD1Migrations) — only author the file.</action>
  <verify>
    <automated>pnpm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE IF NOT EXISTS" migrations/0004_incidents_flagger.sql` == 4 (events, windows, jobs, flags)
    - File contains `CREATE TABLE IF NOT EXISTS events`, `CREATE TABLE IF NOT EXISTS windows`, `CREATE TABLE IF NOT EXISTS jobs`, `CREATE TABLE IF NOT EXISTS flags`
    - `CREATE UNIQUE INDEX IF NOT EXISTS idx_jobs_fingerprint` present
    - `pnpm test` still exits 0 (migration is additive; existing tests unaffected)
  </acceptance_criteria>
  <done>migration 0004 authored with events/windows/jobs/flags tables + indexes; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent code → atlas-incidents queue | Every agent now enqueues RawIncident fire-and-forget; the queue is internal (no external producer) |
| D1 schema migration | New tables added to the shared atlas-db; no data migration of existing rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | RawIncident enqueued by a buggy caller (malformed) | mitigate | RawIncidentSchema (zod) defined here; Flagger (02-02) validates on consume and ack()s malformed (no poison loop) |
| T-02-02 | Repudiation | flag() loses an incident silently | accept | flag() awaits INCIDENTS.send; queue durability + atlas-incidents-dlq (provisioned in 02-02) backstop; low value to spoof |
| T-02-03 | DoS | flag() rework accidentally adds a second atlas-wire consumer | mitigate | This plan adds PRODUCERS only; acceptance criteria assert no atlas-incidents consumer + atlas-wire consumer remains steward-only; guard-wire-consumer.js hook backstops |
| T-02-SC | Tampering | npm installs | mitigate | No new package installs in this plan (rss-parser/cheerio land in Scout 02-04); RESEARCH Package Legitimacy Audit table present |
</threat_model>

<verification>
- `pnpm -r typecheck` exits 0
- `pnpm test` exits 0 with >=315 tests (no regression)
- `grep -rn "queues.consumers" apps/*/wrangler.jsonc | grep atlas-wire` → only apps/steward
- `grep -rn "INCIDENTS.send" packages/shared/src/flag.ts` → present; no `env.WIRE` in flag.ts
</verification>

<success_criteria>
flag() enqueues RawIncident to atlas-incidents; all callers migrated with kind tags; every Phase-0/1 wrangler has the INCIDENTS producer; 0004 migration authored; full suite green; Steward remains sole atlas-wire consumer.
</success_criteria>

<output>
Create `.planning/phases/02-weekly-value/02-01-SUMMARY.md` when done
</output>
