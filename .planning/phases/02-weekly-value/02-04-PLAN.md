---
phase: 02-weekly-value
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/scout/src/index.ts
  - apps/scout/src/sources.ts
  - apps/scout/src/score.ts
  - apps/scout/wrangler.jsonc
  - apps/scout/package.json
  - apps/scout/vitest.config.ts
  - apps/scout/test/idempotent.test.ts
  - apps/scout/test/safety.test.ts
autonomous: true
requirements: [WEEKLY-01]
user_setup:
  - service: scout-config
    why: "Owner-curated event sources + interest keywords (optional starter seed ships in code)"
    env_vars: []
    dashboard_config:
      - task: "Optionally seed CONFIG scout/sources (RSS/HTML URLs) and scout/interests (keyword list)"
        location: "Cloudflare KV CONFIG namespace"
must_haves:
  truths:
    - "Scout.weekly(date) produces an events digest from RSS + plain HTML + Gmail Type/Newsletter and Type/Events (no Browser Rendering, D2-09)"
    - "Each surfaced event is written to D1 events and upserted to Steward via atlas-wire (op:upsert)"
    - "Re-running the same Friday yields the same event list — no duplicate Steward events (cross-week dedupe)"
    - "Scout never follows links from email sources and never reads Type/Security or Phishing-Suspect threads"
    - "Relevance is scored against Codex skills/projects + a KV keyword list (D2-12); no Codex schema change"
  artifacts:
    - path: "apps/scout/src/index.ts"
      provides: "Scout WorkerEntrypoint weekly() + scheduled() + digest event builder"
      exports: ["Scout", "default"]
    - path: "apps/scout/src/sources.ts"
      provides: "RSS/HTML/Gmail event fetchers (injectable ScoutSources interface)"
      contains: "ScoutSources"
    - path: "apps/scout/src/score.ts"
      provides: "pure relevance scoring vs Codex + KV keywords"
      contains: "relevance"
  key_links:
    - from: "apps/scout/src/index.ts"
      to: "atlas-wire"
      via: "send() events.digest + per-event upsert"
      pattern: "send\\(env"
    - from: "apps/scout/src/index.ts"
      to: "D1 events table"
      via: "INSERT OR REPLACE into events"
      pattern: "INTO events"
---

<objective>
Build Scout (#6): the Friday 16:00 weekly events digest. Fetch events from RSS feeds, plain-HTML/JSON listings, and Gmail `Type/Newsletter` + `Type/Events`/`Events/Invite` threads (already labeled by Filer) — NO Browser Rendering (D2-09). Score relevance against the Codex `skills`/`projects` + a KV keyword list (D2-12), persist surfaced events to D1 `events`, and emit `op:"upsert"` events to Steward (Vault Upcoming-events view). Read-and-summarize only — Scout never registers (Usher is Phase 4).

Purpose: WEEKLY-01's events half. Reuses Phase-1 plumbing (Google MCP gmail.readonly, Codex reader, @atlas/wire).
Output: `apps/scout` Worker + injectable sources + pure scorer + idempotency + safety tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-weekly-value/02-CONTEXT.md
@.planning/phases/02-weekly-value/02-RESEARCH.md
@.planning/phases/02-weekly-value/02-PATTERNS.md
@.planning/phases/02-weekly-value/02-VALIDATION.md
@.planning/phases/02-weekly-value/02-01-SUMMARY.md

<interfaces>
From @atlas/shared (after 02-01): `localDate(env?)`, `contentHash(input)` (djb2), `flag(env, sev, title, detail?, { sourceAgent, kind })` (enqueues to INCIDENTS), `RawIncident`, `Severity`.
From @atlas/wire: `send(env, event)`, `WireEvent`.
From @atlas/codex: `read()` (read-only Codex reader — returns the §11 sections incl. skills/projects/addresses; needs an injected drive.readonly token + fetch; CONFIG holds the Drive file id).
Google MCP gmail.readonly: reuse the existing mcp-google gmail tools surface — query `"label:Type/Newsletter newer_than:7d"` / `"label:Type/Events newer_than:7d"`.

D1 events table (from 02-01 migration 0004): `events(id TEXT PK, title, start, end, location, online INTEGER, url, source, relevance INTEGER, why, horizon, status DEFAULT 'surfaced', digest_date, created_at INTEGER)`; indexes idx_events_start, idx_events_status. Positional `?` binds only.

Idempotency keys (NEVER crypto.randomUUID): digest summary `scout:digest:<date>`; per-event `scout:evt_<date>_<contentHash(title+start)>`.
KV knobs (discretionary defaults): min_relevance 55, dedupe_window_weeks 4, max_per_digest 15, relaxed_min_relevance ~40 for sparse weeks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scout sources (RSS/HTML/Gmail) + pure relevance scorer</name>
  <files>apps/scout/src/sources.ts, apps/scout/src/score.ts, apps/scout/package.json, apps/scout/vitest.config.ts, apps/scout/test/safety.test.ts</files>
  <read_first>
    - .planning/phases/02-weekly-value/02-PATTERNS.md → `apps/scout/src/sources.ts` section (the injectable ScoutSources interface, lines 111-130) + `apps/scout/src/score.ts` section (pure-function transform)
    - apps/filer/src/index.ts (the GmailTools injected-interface pattern that sources.ts mirrors — injected for testability, no live network in unit tests)
    - apps/compass/src/score.ts (pure-function scorer analog — no I/O, testable in isolation)
    - .planning/phases/02-weekly-value/02-RESEARCH.md Pattern 7 (RSS rss-parser + cheerio HTML + Gmail query; D2-09 never-follow-links) + Package Legitimacy Audit (rss-parser, cheerio both Approved/OK)
    - .planning/phases/02-weekly-value/02-CONTEXT.md D2-09 (sources + never read Type/Security/Phishing-Suspect) + D2-12 (Codex skills/projects + KV keyword list)
    - packages/codex (the read-only Codex reader signature)
  </read_first>
  <behavior>
    - Test (safety): fetchGmailEvents builds a query containing `label:Type/Newsletter` or `label:Type/Events` and NEVER `Type/Security` or `Phishing-Suspect`; the parsed candidates carry NO followed-link fetches (sources never fetch URLs found inside email bodies — assert no outbound fetch is made for an email-sourced candidate's link).
    - Test (score): score(candidate, codexSkills, codexProjects, kvKeywords) returns a 0-100 number; a candidate matching a skill/keyword scores higher than an unrelated one; pure (no I/O).
  </behavior>
  <action>Add `rss-parser` (3.13.0) and `cheerio` (1.2.0) to `apps/scout/package.json` dependencies (per RESEARCH Package Legitimacy Audit — both Approved). Create `apps/scout/src/sources.ts` exporting an injectable `interface ScoutSources { fetchRss(url): Promise<EventCandidate[]>; fetchHtml(url): Promise<EventCandidate[]>; fetchGmailEvents(query): Promise<EventCandidate[]> }` plus a default live implementation using `rss-parser` Parser and `cheerio.load`, and a Gmail fetch via the mcp-google gmail.readonly surface querying `label:Type/Newsletter newer_than:7d` / `label:Type/Events newer_than:7d`. The Gmail path MUST NOT follow any link found inside an email body (D2-09 §5.8) and MUST skip `Type/Security`/`⚠ Phishing-Suspect` threads. Create `apps/scout/src/score.ts` exporting a PURE `relevance(candidate: EventCandidate, skills: string[], projects: string[], keywords: string[]): number` (0-100) — keyword/skill/project topic-match, no I/O. The root vitest.workspace.ts uses a glob (apps/*/vitest.config.ts) and auto-discovers the new app once its vitest.config.ts exists — do NOT edit vitest.workspace.ts.</action>
  <verify>
    <automated>pnpm --filter scout test -- safety</automated>
  </verify>
  <acceptance_criteria>
    - `apps/scout/package.json` lists `rss-parser` and `cheerio` (the only two new deps this phase)
    - `apps/scout/src/sources.ts` exports the `ScoutSources` interface; the Gmail query string contains `Type/Newsletter` or `Type/Events` and never `Type/Security`/`Phishing-Suspect`
    - sources.ts never fetches a URL extracted from an email body (no link-following; safety test asserts it)
    - `score.ts` `relevance()` is pure (no fetch/DB import in score.ts: `grep -cE "fetch\(|env\." apps/scout/src/score.ts` == 0)
    - `pnpm --filter scout test -- safety` exits 0
  </acceptance_criteria>
  <done>Injectable Scout sources (RSS/HTML/Gmail, no link-following, no security mail) + pure relevance scorer; safety test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scout weekly() entrypoint — dedupe, D1 persist, Steward upsert</name>
  <files>apps/scout/src/index.ts, apps/scout/wrangler.jsonc, apps/scout/test/idempotent.test.ts</files>
  <read_first>
    - .planning/phases/02-weekly-value/02-PATTERNS.md → `apps/scout/src/index.ts` section (WorkerEntrypoint weekly() + scheduled() + buildScoutEvent + flag() + localDate landmines, lines 37-108) + `apps/scout/wrangler.jsonc` section (full config, lines 142-173)
    - apps/compass/src/index.ts (WorkerEntrypoint scheduled read-and-emit analog — exact shape)
    - apps/compass/wrangler.jsonc (canonical wrangler shape to copy for scout)
    - .planning/phases/02-weekly-value/02-RESEARCH.md Pattern 7 (events D1 record + dedupe logic: skip same url OR same (title,start) within dedupe_window_weeks; sparse-week relaxation) + Shared Patterns (structured idempotency keys, owner-local date)
    - apps/dlq-sink/src/index.ts lines 126-143 (D1 positional `?` INSERT OR REPLACE pattern)
    - migrations/0004_incidents_flagger.sql (the events table columns to bind — created in 02-01)
  </read_first>
  <behavior>
    - Test (idempotent): running weekly(date) twice with the same injected ScoutSources fixture writes each event to D1 events ONCE (INSERT OR REPLACE keyed by `scout:evt_<date>_<hash>` id) and emits the same per-event upserts + one `scout:digest:<date>` summary — the second run produces no NEW event rows and the Steward-bound events are identical (replay-safe; dedupe by url OR (title,start) within dedupe_window_weeks).
  </behavior>
  <action>Create `apps/scout/src/index.ts`: `export class Scout extends WorkerEntrypoint<Env>` with `async weekly(params?: { date?; sources?: ScoutSources }): Promise<WeeklyResult>` calling `runWeekly(env, date, sources)`. `runWeekly` fetches candidates from CONFIG `scout/sources` (RSS/HTML URLs) + Gmail, reads Codex skills/projects + CONFIG `scout/interests` keywords, scores each via `relevance()`, filters by CONFIG `min_relevance` (default 55) with sparse-week relaxation to `relaxed_min_relevance` (~40) if fewer than a floor clear, dedupes against the D1 `events` table (skip same `url` OR same `(title,start)` within `dedupe_window_weeks` default 4), assigns `id = scout:evt_<date>_<contentHash(title+start)>`, `INSERT OR REPLACE INTO events(...)` via positional `?`, `send()` a per-event `op:"upsert"` Wire event (idempotencyKey = the event id) and one `events.digest` summary event (idempotencyKey `scout:digest:<date>`). On failure call `flag(env, "P3", "scout weekly failed", String(err), { sourceAgent: "Scout", kind: "scout_failed" })`. Add the `scheduled()` default export (calls runWeekly) and at end of a successful run emit the `kind:"heartbeat"` incident (`source_agent:"Scout"`, severity_hint "P4"). Create `apps/scout/wrangler.jsonc` copying apps/compass shape: producers WIRE + INCIDENTS; D1 DB; CONFIG KV; AI binding; nodejs_compat; NO own cron (Atlas invokes via service binding in Plan 02-07); staging crons []. NEVER use `new Date()` for the date — use `localDate(env)`.</action>
  <verify>
    <automated>pnpm --filter scout test -- idempotent</automated>
  </verify>
  <acceptance_criteria>
    - `apps/scout/src/index.ts` exports `class Scout extends WorkerEntrypoint<Env>` with a `weekly()` method and a `scheduled()` default export `satisfies ExportedHandler<Env>`
    - Per-event idempotency key matches `scout:evt_<date>_<hash>`; digest key matches `scout:digest:<date>` (no crypto.randomUUID: `grep -c randomUUID apps/scout/src/index.ts` == 0)
    - Events persisted to D1 via positional `?` (no named params)
    - Re-running weekly(date) produces no duplicate event rows and identical Steward-bound events (idempotent test asserts it)
    - `apps/scout/wrangler.jsonc` has NO `triggers.crons` for production (Atlas drives it) and NO consumers block
    - `pnpm --filter scout test -- idempotent` exits 0
  </acceptance_criteria>
  <done>Scout.weekly() fetches/scores/dedupes events, persists to D1, upserts to Steward idempotently; heartbeat emit; idempotent test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSS/HTML source → Scout | Untrusted third-party content; parsed, never executed |
| Gmail email bodies → Scout | Email content is untrusted; links must NOT be followed |
| Scout → atlas-wire / D1 | Internal write path via Steward (upsert) + direct D1 events insert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-link | Elevation of Privilege | Scout following a link from an email source | mitigate | D2-09 §5.8: sources.ts never fetches URLs found in email bodies; never reads Type/Security/Phishing-Suspect; safety test asserts no link-follow |
| T-02-html | Tampering | malicious RSS/HTML payload | mitigate | cheerio parses (no JS execution); rss-parser handles feed quirks; relevance scoring is pure text match, no eval |
| T-02-pii | Information Disclosure | Codex read leaks profile facts | accept | Codex is read-only; Scout reads skills/projects/addresses only; no Codex write; no schema change (D2-12) |
| T-02-SC | Tampering | rss-parser + cheerio installs | mitigate | RESEARCH Package Legitimacy Audit: both [OK]/Approved (8yr/12yr, high downloads, known repos); no [ASSUMED]/[SUS] packages → no blocking checkpoint required |
</threat_model>

<verification>
- `pnpm --filter scout test` exits 0 (safety + idempotent)
- `grep -rn "queues.consumers" apps/scout/wrangler.jsonc` → empty (Scout is a producer only)
- Definition-of-Done: Wire-contract test (events.digest + per-event upsert shape) + replay (Steward meta.changes===0 via existing infra) + failure-path (scout_failed P3) present
- `pnpm test` full suite green
</verification>

<success_criteria>
Scout produces a weekly events digest from RSS/HTML/Gmail (no Browser Rendering), scores relevance vs Codex+KV, persists to D1, upserts idempotently to Steward; never follows email links or reads security mail; safety + idempotent tests green.
</success_criteria>

<output>
Create `.planning/phases/02-weekly-value/02-04-SUMMARY.md` when done
</output>
