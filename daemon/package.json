{
  "name": "atlas-daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Atlas LOCAL macOS launchd daemon — the OUTBOUND-only Obsidian bridge drainer + browser-action runner (NOT a Worker; outside apps/). Echo + Quill capture land in Phase 3.",
  "main": "src/main.ts",
  "scripts": {
    "start": "node --experimental-strip-types src/main.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "playwright": "1.60.0"
  }
}
